[gd_scene format=3 uid="uid://fompglautjp6"]

[ext_resource type="Script" path="res://src/World/CellNavBaker.cs" id="2_navbaker"]
[ext_resource type="Script" path="res://src/Entities/Entity.cs" id="3_entity"]
[ext_resource type="Script" path="res://src/Enemies/LairSpawnComponent.cs" id="4_lair"]

; The geometry the nav bake parses comes from static colliders (Phase 27A) — never visual meshes,
; which force a GPU→CPU readback hitch.
; ⚠️ THE NAV VOXEL GRID COARSENED WITH THE WORLD (the 2026-08-29 geography overhaul). Cells
; went from 52 m squares on a flat slab to 90-200 m of real terrain, and a 0.25 m voxel over
; five times the area is twenty-five times the columns to rasterise — the traversal probe timed
; out waiting for the bake. ⚠️ AND THE agent_* DIMS DID NOT FOLLOW. On the 0.25 grid 1.75 and
; 0.5 were whole voxels; on this one they are not, so every bake quantised them and warned. The
; values below are the QUANTISED ones, so the file now says what the bake does: agent_height and
; agent_radius CEIL to a voxel (cell_height / cell_size), agent_max_climb FLOORS to cell_height.
; ⚠️ They encode this grid — change cell_size or cell_height and recompute all three with it.
[sub_resource type="NavigationMesh" id="NavMesh_roost"]
geometry_parsed_geometry_type = 1
cell_size = 0.3
cell_height = 0.3
agent_height = 1.8
agent_radius = 0.6
agent_max_climb = 0.3
agent_max_slope = 42.0

[node name="RoostCell" type="Node3D"]

[node name="Nav" type="NavigationRegion3D" parent="."]
navigation_mesh = SubResource("NavMesh_roost")

[node name="NavBaker" type="Node" parent="Nav"]
script = ExtResource("2_navbaker")

[node name="Nest" type="Node3D" parent="."]
script = ExtResource("3_entity")
DisplayName = "Roost"
PersistentId = "lair.roost"

[node name="Lair" type="Node" parent="Nest"]
script = ExtResource("4_lair")
SpawnOffset = Vector3(0, 0.5, 0)
