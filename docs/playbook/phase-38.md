## Phase 38 — Economy, Vendors & Services `[F/C]`

- [x] **38A — `VendorComponent` + `ShopResource` (buy/sell)** `[F]` ✅
  - **Done when:** buy/sell works against the item system with buy/sell spreads.
  - **Landed:** `src/Economy/` — `ShopResource` (wares + `BuyMarkup`/`SellFraction`), `ShopDatabase`,
    `VendorComponent`, and `ShopPricing`, the Godot-free core that owns every price so
    `ShopPricingTests` can pin the rounding. `VendorPanel` is `StoragePanel` with prices, on the
    37.5C item vocabulary. One authored shop: `shop.ember_crown.goods`.
  - ⚠️ **The stub vendors are untouched, deliberately.** `EntityNode.GetComponent<T>` returns the
    *first* child match, so a `VendorComponent` behind a `DialogueComponent` on the same actor never
    fires. Whether trade replaces the conversation or hangs off a new `OpenShop` dialogue effect is
    **38A's decision to not make** — it is 38E's. Reach a shop meanwhile with `shop <id>` (F1), which
    publishes the same `ShopOpenedEvent` a placed vendor does.
  - Three traps worth carrying into 38B–38E:
    1. **Buy must charge before it delivers.** `AddInstance` merges a stackable into an existing
       stack, so the instance you hand it is often never stored and `RemoveOneInstance` has nothing
       to roll back. Refunding gold always works; rolling back an item does not.
    2. **A zero payout is refused, not accepted.** `SellPrice` floors at 0, so a 1-value common at a
       0.4 fraction pays nothing — accepting it would be item loss dressed as a transaction.
    3. **The spread rule is enforced twice**, in the validator *and* clamped in the arithmetic. Data
       alone is not enough for something whose failure mode is infinite gold.
- [x] **38B — Stock: static + restock + leveled** `[F]` ✅
  - **Done when:** vendor stock supports static lists, restock timers, and leveled
    pools.
  - **Landed:** `ShopStockEntry` sub-resources (`ItemId` + `Quantity`) replace 38A's flat id list —
    `Quantity = 0` is the static unlimited row, above it is finite. `RestockDays` is the clock,
    `LeveledTable` is a `LootTable` rolled through `LootGenerator` at `ShopStock.QualityForLevel`.
    `ShopStockService` (`ISaveable`, `"shopstock"`) holds and persists remaining counts, restock stamps
    and rolled wares. `WorldClock` gained `Day`.
  - **`WorldClock` had no date at all.** `TimeOfDay` wraps through `PosMod` and nothing counted the
    wraps, so "three days later" was inexpressible — a 3-line addition that every future system can
    key off. `SetTimeOfDay(26)` rolls the date forward, which is the only way to advance a day without
    waiting 180 real seconds for each one.
  - **This is the game's first player-level-driven scaling.** Nothing else reads
    `ProgressionComponent.Level` to scale anything, despite `LootRarity.Select`'s comment having claimed
    for several phases that quality came partly from "enemy level". It moves rarity and affixes only;
    *what* a merchant deals in stays authored.
  - Four things worth carrying into 38C–38E:
    1. **Restock is lazy-on-open, not ticked.** No `_Process`, no event, no `DayChangedEvent`. Nothing
       can observe the difference, and `WorldEventDirector` shows the cost of the alternative — it ticks
       real-seconds cooldowns every frame and is not `ISaveable`, so they vanish on reload.
    2. **Day arithmetic must widen to `long`.** A never-stocked shop is stamped `int.MinValue` and
       `0 - int.MinValue` overflows back negative, so the obvious version answered "not due" for a shop
       that had never stocked. The test written for it failed on the first run; that is the only reason
       it is not in the build.
    3. **A clock *behind* the stamp must also restock.** A quickload rewinds the world clock while the
       service may still hold a stamp from the abandoned timeline; comparing only forwards freezes that
       shop for the rest of the run, and it looks exactly like the feature not working.
    4. **The rolled wares are in the save on purpose.** Without them a reload rerolls the pool and the
       player reloads until a Legendary appears.
- [x] **38C — Reputation discounts + gold sinks** `[F/C]` ✅
  - **Done when:** faction standing modifies prices; defined gold sinks exist.
  - **Landed:** `ShopResource.FactionId` + `ShopPricing.PriceMultiplierFor`/`MarkupFor` (a surcharge
    across the hostile half of the ramp down to 15% off at Allied); a hostile faction refuses to trade
    via `ReputationComponent.IsHostile`; `ShopResource.PurseGold` held and persisted by
    `ShopStockService` and refilled on 38B's restock clock; and `TravelFee`/`TravelCosts` — fast travel
    costs gold, free to a holding you own. `docs/DESIGN.md` §6 now carries the authoritative sink table.
  - **Reputation had never changed a number.** `ReputationComponent` shipped in Phase 16 and had exactly
    two behavioural readers, both asking the same boolean (`IsHostile`); standing was otherwise written
    by quests and displayed on the character screen. 38C is the first tier read in the game.
  - Three things worth carrying into 38D–38E:
    1. **A shop must invert the AI's fail-safe.** `EnemyAIComponent` treats a missing
       `ReputationComponent` as *hostile* — correct for something deciding whether to swing, and it would
       make every merchant in a half-built world refuse. Any new standing-gated *service* needs the same
       inversion.
    2. **A price shown must be the same function call as the price taken.** The first draft of the travel
       fee read the active region from a service the map screen could resolve and the bootstrap could
       not, so the button would have promised a local fee while the jump charged the cross-region one.
       The `--play` run caught the symptom (a duplicate-registration warning) and the premise turned out
       to be wrong entirely — the streamer was already registered by the bootstrap.
    3. **Put arithmetic where a test can reach it.** The purse logic started inside `ShopStockService`,
       which is a Godot `Node` the test project cannot construct, so it was untestable *and* the save
       round-trip could not be driven headlessly either. Moving `CanCover`/`AfterSpend`/`AfterRefund`
       into `ShopStock` made the risky half provable. 38D's services will have the same shape.
  - ⚠️ **`DESIGN.md` §6 carries an authored contradiction** — "training/perks-for-pay" against "perks are
    bought with skill points, **not gold**". 38C left the rule alone and wrote down the reading that
    holds (a trainer sells *access*, never a perk rank). **38D has to resolve it.**
- [x] **38D — Services: trainer / bank / inn / stable** `[F/C]` ✅ *(repair deferred to 40A)*
  - **Done when:** trainer (buy perks/points), bank (storage), innkeeper (rest/
    time-skip), stablemaster (mounts stub), and repair (if durability adopted in 40)
    are interactable services.
  - **Landed:** `ServiceResource` + `ServiceKind` + `ServiceDatabase` + one `ServiceComponent` branching
    on kind (the `WorldEventDirector` shape), with the pure half in `ServiceRules` and the price on
    `ShopPricing.ServicePrice`. Four authored services in the town hub: the inn on the innkeeper, a
    trainer and a stablemaster as new NPCs, and the vault as a prop.
  - **The trainer is the first caller `CraftingComponent.Learn` has ever had.** It sat with zero callers
    from Phase 15, which is why `GameIds.Recipes.Starting` was the entire reachability guarantee and how
    `recipe.leather_vest` rotted for twenty phases. `recipe.drakescale_mail` moved *out* of `Starting`
    to be taught — it only started there because nothing could teach a recipe, and gating it on eight
    dragon scales was the workaround for that.
  - **`--validate` now checks recipe reachability as a union** (`Starting` ∪ every trainer's list) and
    rejects the overlap too: `PlayerFactory` seeds `Starting` unconditionally, so a recipe in both is a
    trainer selling knowledge the player walked in with.
  - **Repair is deferred, not skipped.** No durability or condition concept exists in the game at all,
    40A decides whether it ever will, and 40B's rule is that cut systems leave no stub — so there is no
    `ServiceKind.Repair` member. Recorded in `DESIGN.md` §6's sink table as *pending 40A*.
  - **38C's authored contradiction is resolved** (`DESIGN.md` §6): a trainer sells **access, never a
    rank**. Recipes and XP, so points arrive by levelling; `ProgressionComponent` gained no new API.
  - Three things worth carrying into 38E–39:
    1. **`WorldClock.SetTimeOfDay` needs `RestHour + 24`.** It advances `Day` only for an hour ≥ 24 and
       otherwise just rewinds the hour, so an inn passing its authored hour straight through would look
       like it worked while silently freezing 38B's restock clock. `ServiceRules.RestTarget` owns the +24
       and a swept test proves no starting hour can produce a backwards jump.
    2. **Which services need an `UnlockFlagId` is a validator rule, not a convention.** A Bank or Stable
       without one re-charges forever; a Trainer granting XP without one is an infinite gold-to-levels
       pump; an Inn *with* one only ever charges for the first night. All three are well-formed data and
       a broken economy, which is exactly what the validator is for.
    3. **The one-interactable trap bit again.** The innkeeper's `DialogueComponent` had to be removed
       outright for the service to fire. That was affordable only because it was the shared placeholder
       stub — **38E still owns the real decision** for the three shop vendors, whose conversations are
       genuine content.
- [x] **38E — Wire real shops into Ember Crown vendors** `[F/C]` ✅
  - **Done when:** the Phase 27 stub vendors become real shops; `validate` green.
  - **Landed:** `DialogueEffect.OpenShop` (ordinal 9) and one case in `DialogueSession.ApplyEffect` that
    publishes the existing `ShopOpenedEvent`. Two authored shops — `shop.ember_crown.smith` (Bryn) and
    `shop.ember_crown.apothecary` (Mirela) — plus `dialogue.vendor_goods`, Aldreth's real conversation,
    replacing the hard-coded `dialogue.vendor_stub`. A trade choice on all three merchants. **No new C#
    file, no new component, no new panel:** the whole feature is one enum member, one switch case and
    authored data, and the `.tscn` changed by exactly one string.
  - **The one-interactable decision reserved since 38A is made: the effect, not the component.** Replacing
    the dialogue was the cheaper diff and the wrong answer — `dialogue.smith` and `dialogue.apothecary`
    carry `quest.warband.forge` and `quest.warband.remedies`, so it would have relocated live quest content
    to make room for a menu. It is also load-bearing forward: **a merchant who is a person can carry hours,
    a haggle, a contract and a rumour; a `VendorComponent` on a crate cannot.** `VendorComponent` keeps its
    place for unattended counters and stalls, first placed in 38K.
  - **A shop id is validated for the first time in the game's history.** `ContentValidator` does not scan
    `.tscn`, so a mistyped `VendorComponent.ShopId` has always given *no prompt at all* rather than an
    error. An `EffectArg` lives in a `.tres`, so `ValidateDialogue` can read it — the new rule is four lines
    beside the `LearnSpell` check it copies.
  - ⚠️ **An `OpenShop` choice must leave `Goto` empty, and the validator now enforces it.** A conversation
    left open behind the vendor window returns the moment the shop closes — the player closes a shop and is
    back in a dialogue they thought they had left. This is well-formed data and a broken interaction, which
    is exactly what a validator rule is for.
  - **The panel handover is safe because `UiState` counts owners.** `Choose` applies the effect *before* it
    resolves `Goto`, so `VendorPanel` registers with `UiState` before `DialoguePanel` deregisters and the
    count never reaches zero — no pause flicker and no mouse-mode flicker between the two windows. Had the
    order been reversed, the world would have unpaused and recaptured the mouse for one frame mid-handover.
  - **Two merchants now pay differently for the same item** (Bryn's `SellFraction` 0.45 against Aldreth's
    0.4, because a smith knows what metal is worth). That is the smallest possible version of 38F's
    specialist premium, authored rather than coded, and it is the first time in the game that *where* the
    player sells has mattered at all.
  - Three things worth carrying into 38F:
    1. **The stub was a locale violation, not just filler.** `dialogue.vendor_stub` held hard-coded English
       in a `.tres` — `Loc.T` on a literal returns the literal, so it renders correctly and no rule catches
       it. Grep new content for prose where a key belongs; the validator only checks keys it is *told* about.
    2. **`GameIds` constants outlive what they name.** `Dialogues.VendorStub` had zero consumers beyond
       itself and would have kept a deleted file's id alive in code indefinitely. A dialogue id is safe to
       rename (nothing persists it); an item or flag id in the same position would have been a migration.
    3. **The effect route is the pattern for every conversational service too** — but `OpenService` was
       deliberately *not* appended here, because firing a service without a `ServiceComponent` means
       refactoring its pay-and-grant path, and an enum member with no implementation is the stub 40B
       forbids. It lands in 38R with the innkeeper's conversation.
  - Build clean + **972** tests + `--validate` exit 0 with both new rules negative-tested in both
    directions + a clean `--play` boot (3 shops, 13 conversations, town hub streamed).
  - **Reachable from `--play`:** talk to Aldreth, Bryn or Mirela in the town hub and pick the trade line.
    `shop <id>` still reaches all three from the F1 console; `rep faction.villagers <delta>` drives the
    discount; `shop restock <id>` skips the clock.

---

### Phase 38, continued — the economy overhaul arc `[F/C]`

> Phase 38 built the machinery and 38E made it reachable. This arc is the final economy pass: it turns a
> set of working shop systems into a reason to travel, to choose, and to keep playing after the gear stops
> improving. Scoped to `region.ember_crown` — commerce in the other realms is Phase 44's content, all
> numbers are Phase 56's, and durability/repair remains 40A's to adopt or cut.
>
> Design foundations, pricing chain and the full roster live in the approved arc plan; the load-bearing
> engineering rule is that **`SELL < BUY` must hold across every multiplier**, which is why demand is
> applied symmetrically to both sides and saturation only ever reduces the sell side. Every new multiplier
> joins the invariant sweep test or it does not ship.
>
> **Cut line:** 38F–38J plus 38U and 38V is the coherent minimum economy. The rest is depth, each entry
> independently shippable.

- [x] **38F — Trade tags + merchant specialties** `[F]` ✅ *(no `TradePrice` class — see below)*
  - **Done when:** items carry trade tags, shops accept or refuse by tag and pay a specialist premium, and `TradePrice` is the single price authority with a multiplier-sweep invariant test.
  - **Landed:** `src/Economy/TradeTags.cs` (a closed 17-word vocabulary plus the accept/specialty rules, Godot-free), `ItemResource.TradeTags`, `ShopResource.AcceptedTags`/`Specialties`, two constants and one new function on `ShopPricing`, a fourth named refusal in `VendorPanel`, four validator rules, and all 25 sellable items tagged. Bryn deals in metal, weapons and armour; Mirela in herbs, reagents and remedies; Aldreth in everything.
  - **The planned `TradePrice` class was cut, and that is the sub-phase's main finding.** The arc plan wanted a new price authority because five multipliers make `SELL < BUY` non-obvious. Reading `ShopPricing` closely, it is already obvious and already proven: `BuyPrice` clamps its markup to `>= 1` and `SellPrice` clamps its fraction to `0..1`, so **`sell <= value <= buy` holds for any multiplier**, by construction, and any new one folded into the markup or the fraction inherits it for free. A new class would have been a rename across `VendorPanel`, `ServiceComponent`, the tests and four docs for zero added safety. **38G onward should not re-litigate this** — extend `ShopPricing`.
  - **What the clamps do *not* prevent is a spread closing to nothing.** `sell == buy` is not a money printer, it is frictionless churn, and it is invisible until a player notices a round trip is free. That is a content bug the arithmetic cannot report, so `ValidateShopTrade` holds every shop's widest possible sell against its narrowest possible buy with a 1.25× margin.
  - ⚠️ **Both empties mean yes, deliberately.** An empty `AcceptedTags` is how a general store is authored, and an untagged item is accepted everywhere. Both fail *open*, matching the inverted fail-safe the shops already use for a missing `ReputationComponent` — the alternative turns every authoring gap into unsellable loot, and the symptom looks exactly like the feature being broken. It also meant the two new fields landed **without changing a single existing merchant**.
  - **Tags rather than an `ItemType` member**, for two reasons that both matter: `ItemType`'s ordinals are persisted in every save so appending is irreversible (37C made the same call), and an item wears several trades at once — a leather cap is `armor` *and* `leather`, which no single enum member can say.
  - Three things worth carrying into 38G:
    1. **The vocabulary ships only tags something wears today.** Ten more are named in the arc (`contraband`, `food`, `fish`, `textile`, `tome`, `map`, `livestock`, `fuel`…) and each arrives with the sub-phase that authors a member for it. A tag with no members is a promise, not a feature.
    2. **The sweep test is the arc's safety rail, and it is a contract.** `NoCombinationOfMultipliersLetsSellingBeatBuying` runs every tier × markup × fraction × specialty × value. ⚠️ **Every future price multiplier joins it or it does not ship** — that is what lets 38G add regional demand without re-deriving the argument. Note demand must be applied *symmetrically* to both sides, or it moves the ratio rather than the price.
    3. **A refusal that names a trade is a tutorial nobody had to write.** "They have no use for this" leaves the player carrying a pelt around a town; "Bryn deals in metalwork, weapons and armour" tells them what kind of person to look for. The whole specialty system is taught by being refused once.
  - Build clean + **981** tests + `--validate` exit 0 with all four new rules negative-tested in both directions + a clean `--play` boot.
  - **Reachable from `--play`:** sell a healing herb to Bryn (refused, by trade) and to Mirela (a premium); sell iron ore to Bryn and to Aldreth and compare. `shop <id>` reaches all three from F1.
- [ ] **38G — `RegionEconomyResource`: settlement demand** `[F/C]` ⏸ **deferred until after 38K/38N**
  - **Done when:** each commercial POI has authored surplus/demand tags that move prices symmetrically on both sides.
  - ⚠️ **Do not build this next.** Demand prices goods by *where you are*, and every shop in the game is in one settlement — the multiplier would read identically in all three, so the system would be correct, validated, saved and completely imperceptible. It needs a second market to contrast against, which 38K (the Embermarket) and 38N (the mine and the fishing hamlet) create. 38H was brought forward in its place.
- [x] **38H — Market saturation** `[F]` ✅ *(taken out of order, ahead of 38G)*
  - **Done when:** repeat sales of one item to one merchant decay toward a floor and recover on the restock clock.
  - **Landed:** `ShopStock.SaturationMultiplier`/`SaturatedPayout` (pure), an `Absorbed` count per shop per template in `ShopStockService` cleared at restock and carried in the save, and the payout path in `VendorPanel` reading it. **No new file, no new resource field, no authoring** — saturation is entirely emergent from the three shops that already existed, which is why it was the right entry to bring forward.
  - **It caps nothing.** No limit, no daily allowance, no refusal: the price falls and the player decides whether it is still worth the walk. A hard cap would make the mechanic a punishment; a slope makes it a decision, and the decision it pushes toward — another buyer, another town, come back tomorrow — is the whole arc's goal wearing a price tag.
  - ⚠️ **A stack decays across its own units, and that granularity is the feature.** A stack sale used to be one price multiplied by a quantity; applying saturation at that level prices all twenty hides at the pre-sale multiplier and makes "dump the whole stack in one click" strictly optimal — saturation would then punish only the player who sells tidily. `SaturatedPayout` sums per unit as the count climbs, and `StackSaleMatchesSellingOneAtATime` pins the equivalence.
  - ⚠️ **Two edge cases the first draft got wrong, both caught by tests rather than by reading:**
    1. **No grace band meant an honest haul was docked.** With decay starting at the first unit, an ordinary post-hunt stack of ten took a 30% haircut on the *first* sale of the day. `SaturationGrace` (8 units at full price) moves the pressure onto repeat volume, where it belongs — the third trip back with the same goods, not the first.
    2. **Each unit must floor at 1, not 0.** A goblin hide sells for a single coin, so *any* multiplier below 1 rounds it to nothing — and a zero payout is refused as worthless (38A, deliberately). Saturation would have quietly become a *refusal* for exactly the cheap high-volume goods it is about, and the symptom reads as the merchant breaking.
  - **No validator rule, deliberately.** The only failure mode saturation introduces is permanent decay on a shop with no restock clock, and `SaturationMultiplier` answers that by returning `1f` — the impossible case is settled in the arithmetic instead of rejected in the data, so there is no second authority to drift.
  - **38F's sweep contract honoured on its first opportunity:** saturation joined `NoCombinationOfMultipliersLetsSellingBeatBuying` as a new dimension. It can only ever reduce the sell side, so the invariant was never at risk — the point is that the next multiplier's author finds the habit already established.
  - Two things worth carrying into 38I:
    1. **One clock now carries three things** — the shelves, the purse and the market's appetite all reset on `RestockDays`. Anything else that needs to decay and recover should ride it rather than adding timing machinery; `WorldEventDirector` remains the cautionary tale.
    2. **Cheap items are where price maths breaks.** Two of this sub-phase's three bugs were about a 1-gold item, and neither was visible when reasoning with a 10-gold one. Test the coin, not the ingot.
  - Build clean + **987** tests + `--validate` exit 0 + a clean `--play` boot.
  - **Reachable from `--play`:** `give item.material.goblin_hide 30`, then sell to Aldreth in batches and watch the per-unit payout fall and the "plenty already" chip appear; `shop restock shop.ember_crown.goods` or `time 26` clears it.
- [x] **38I — Stock tiers + merchant investment** `[F]` ✅
  - **Done when:** stock can be gated by standing, flag or investment, and gold buys a stake that raises a merchant's purse and unlocks tiers.
  - **Landed:** three optional gates on `ShopStockEntry` (`RequiredTier` / `RequiredFlagId` / `RequiredInvestment`), a `ShopInvestmentTier` sub-resource and `ShopResource.InvestmentTiers`, four pure functions on `ShopStock` (`LockOf`, `PurseBonusThrough`, `PurseAfterInvestment`, plus the `StockLock` enum), an `Investment` count per shop in `ShopStockService` carried in the save, an Invest line and locked rows in `VendorPanel`, nine validator rules, `shop invest <id>`, and gates authored on all three Ember Crown merchants. **One new file**, and it holds two exported ints.
  - **The defaults are the ungated case, and that is why the three fields landed without touching a single existing shop.** `ReputationTier.Hated` is the bottom of the ramp, an empty flag is no flag, and zero rungs is no stake — so no sentinel value was invented for "no gate", the same trick `ShopStockEntry.Quantity = 0` already plays for unlimited stock. 38F's `AcceptedTags` made the same call from the opposite direction (both empties mean yes).
  - ⚠️ **A stake deliberately adds no price multiplier, and this entry says so loudly so the next author does not think it was forgotten.** 38F's sweep contract is honoured by there being nothing to add: standing owns the price ramp, a stake owns *capacity and access*. An investor discount was considered and cut — it duplicates reputation from a second authority that can drift, and it would have had to earn its place in `NoCombinationOfMultipliersLetsSellingBeatBuying`. **A later sub-phase that wants one joins that test.**
  - **The gate is a slope, not a wall, in the same way 38H's saturation is.** A locked row stays on the shelf, greyed, with its gate named — the sold-out rule from 38B, for a stronger reason: a hidden row teaches nothing, while a locked one is the only way a player ever learns the shelf goes deeper. ⚠️ The refusal order is **flag → standing → gold**, lifted straight from `PropertyClaim.Resolve`: a player must never be told to go and earn coin for something a story beat is holding shut.
  - ⚠️ **An unlimited purse stays unlimited no matter the stake.** `PurseGold = 0` means the merchant buys anything; adding a bonus to it would make her *finite*, which is a downgrade the player just paid for. Settled inside `ShopStock.PurseAfterInvestment` so no future caller can forget it (38H's never-restocks guard, again) **and** rejected by `--validate`, because arithmetic being safe does not make the data meaningful. The two are not redundant: one protects the run, the other protects the author.
  - ⚠️ **`RefundPurse` had to move to the invested ceiling.** It clamped to `shop.PurseGold`, which was right until a purse could exceed it — a sale that debited and then failed to remove the item would have refunded the merchant back down to her pre-stake ceiling and silently erased what the player bought. Caught by reading the call, not by a test; pinned by one afterwards.
  - **Mirela sells no stake, and that is the design rather than an omission.** She gates a reagent behind the Iron King's own `DefeatFlagId` and the alchemy kit behind Honored standing. A gate does not need gold behind it — trust, a deed done, and a stake are three different reasons, and making all three cost money would have collapsed them into one.
  - **The flag gate cost four lines because `ValidateStoryFlags` already existed.** Shop rows became the third reader family beside dialogue conditions and `RegionResource.UnlockFlagId`, reusing the `written` set the pass already builds. A mistyped flag here is the quietest failure in the sub-phase — a shelf that stays greyed forever behind perfectly sensible prose.
  - Two things worth carrying into 38J:
    1. **A gate is cheap; the authoring around it is not.** Every one of the nine validator rules rejects data that is well-formed and buys the player nothing, and five of them exist because a gate can be *unreachable* in a way nothing in game reports — a row above the ladder, a standing gate on a factionless shop, a shop that gates every row and opens empty. Trading hours are the same shape: a closed shop is a gate on a clock, and "no closed shop hard-gates an essential" is that same unreachability question wearing a different hat.
    2. **`git checkout -- <dir>` while negative-testing eats uncommitted authoring.** Half a session's `.tres` work went in one revert between rule 1 and rule 2. Commit the sub-phase *before* breaking data on purpose; the negative pass is a destructive test and deserves the same care as one.
  - Build clean + **995** tests + `--validate` exit 0, with all nine new rules negative-tested in both directions (each firing its own message on broken data, all silent on the authored data). ⚠️ **No `--play` walk this session** — the maintainer asked for code-only verification, so boot, save-restore and the in-window behaviour of the Invest button are *reviewed against the Godot 4.7 C# API*, not observed.
  - **Reachable when next played:** `shop shop.ember_crown.smith` shows two greyed rows naming their gate; `shop invest shop.ember_crown.smith` twice opens both and prints the raised purse; `shop restock shop.ember_crown.smith` proves the purse refills to the *invested* figure; `rep faction.villagers 60` opens Mirela's alchemy kit; killing the Iron King opens her dragon scale.
- [x] **38J — Trading hours + travelling merchants** `[F]` ✅
  - **Done when:** shops open and close on the world clock and a merchant arrives and leaves on a day cycle, with no closed shop hard-gating an essential.
  - **Landed:** `src/Economy/ShopHours.cs` (five pure functions and two design constants), four fields on `ShopResource`, a `ShopOpen`/`ShopClosed` `DialogueCondition` pair with two explicit cases and a backstop in `DialogueSession`, presence + two refusals on `VendorComponent`, eight validator rules, hours on all three residents with a closed-hours line each, and **Hesk the Wayfarer** — `shop.ember_crown.traveller` plus the first `VendorComponent` ever placed in the world.
  - **Presence needs no save state at all, and that is the sub-phase's best decision.** A merchant's whereabouts is a pure function of `WorldClock.Day`, so there is nothing to persist, nothing to load-order, and nothing to drift out of step with a rewound clock. Every other piece of shop state in this arc is in the save because it *cannot* be recomputed; this one can, so it is not. ⚠️ The rule to carry: before adding a field to `ShopStockService.Save`, ask whether the fact is derivable from the clock.
  - ⚠️ **Hiding a `Node3D` does not disable its collision.** The first draft toggled `Visible` alone, which leaves the absent trader stopping the interact raycast and the player's own body — an invisible wall in the middle of the square that reads as a physics bug and never as a merchant being away. `ApplyPresence` zeroes and restores the collider's `CollisionLayer` in the same function, cached per collider so a scene authoring its own layers is restored to *those* rather than to a constant. A merchant who came back on the wrong layer is the same bug inverted: a ghost the raycast passes through.
  - **The refusal speaks because a condition makes it speak.** The backstop in `ApplyEffect` refuses silently by design, so on its own it would turn "let's trade" at midnight into a choice that does nothing — a dead end, not a refusal. The `ShopOpen`/`ShopClosed` pair (`HasFlag`/`MissingFlag`'s shape) is what puts the closing time in the merchant's own mouth, and `--validate` requires the pairing on any shop with hours. ⚠️ **Both of Aldreth's trade choices needed it** — he offers trade on the root node and again after the road talk, and a gate on one of two doors is not a gate.
  - ⚠️ **`Evaluate`'s `default: return true` is a trap for every future condition member.** An unhandled member falls through to *shown*, so a trade line would appear at 02:00 — the precise failure this sub-phase exists to remove. Both new members are spelled out explicitly rather than relying on the fall-through being correct today.
  - **Hours are a wait; a travelling merchant is a coin flip.** That distinction is the whole of the "no closed shop hard-gates an essential" rule. A player told to come back at dawn sleeps at the inn and the clock moves; a player out of potions cannot sleep their way to a merchant who is not in town. So the validator's essentials rule is scoped to `ItemType.Consumable` and to *travelling* shops only — using an existing enum member that already means "the thing you run out of mid-fight", so there is no new authored marker and no judgement call about what counts.
  - **Hesk carries the realm's second leveled pool**, which 38B explicitly refused Bryn. That argument ("a second one halves how special it is") was about a second *resident* shelf standing open every day; a pool rolled for a trader who is here a quarter of the time is the opposite — it is the reason to catch him, and it cannot be shopped until something Legendary appears, because he leaves.
  - ⚠️ **Nothing can validate that a shop's hours match its merchant's routine.** A `ScheduleId` lives in a `.tscn` and `ContentValidator` does not scan those, so Bryn's 07:00–19:00 and `schedule.vendor_smith` are kept in step **by hand**, and both `.tres` files say so. If his routine moves and the hours do not, the player finds him hammering at a forge that refuses to sell.
  - Two things worth carrying into 38K:
    1. **`VendorComponent` finally has a world placement, and it was the right shape for exactly the reason 38E predicted.** The one-interactable trap forced the three residents onto dialogue effects because their conversations carry quest content; Hesk has no conversation to protect, so the component is simply correct. The Embermarket's stalls are the same case at a dozen times the scale.
    2. **The console must say when it overrode the world.** `shop <id>` deliberately opens a shut shop and an absent merchant's shelves — waiting three real minutes per in-game day to inspect a stall is not a test loop — but a window that opened while the world said "closed" is exactly the sort of thing that gets reported as the hours being broken, so the command names the override in its output.
  - Build clean + **1005** tests + `--validate` exit 0, with all eight new rules negative-tested in both directions. ⚠️ **No `--play` walk** — code-only verification at the maintainer's standing request, so the presence toggle, the collider restore and the closed-hours conversation are *reviewed against the Godot 4.7 C# API*, not observed.
  - **Reachable when next played:** `time 2` then talk to Bryn — the trade line is gone and he tells you the forge is cold; `time 9` and it is back. `shop shop.ember_crown.traveller` opens Hesk's cart from the console and says whether he is actually in town; `time 26` rolls the day until he is.
- [x] **38K — The Embermarket cell** `[C]` ✅
  - **Done when:** a market-district cell streams, navigates, lights and carries a travel node.
  - **Landed:** `scenes/regions/ember_crown/embermarket.tscn` — a 52 m flagged square one street south of the town hub, six stalls facing each other across a lit aisle, houses closing the east and west edges, and an attunable waystone. Plus `RegionCellResource.SafeRadius`, a `SafeZones` that holds a *list*, `GameBootstrap.ApplySafeZones`, a locale key for the travel node's name, and four validator rules.
  - **The stalls are named and empty on purpose.** 38K builds the *place*; the twelve specialist merchants are 38L's. `StallW1..StallE3` are laid out and named now so that placing a vendor next session is a transform copied out of this file rather than a position guessed in the dark — the same split 38A/38E made between a shop and the merchant who stands at it.
  - **`SafeZones` became a list, which is exactly what its own ponytail note asked for.** It read *"one zone per region; make it a list when a second safe area actually exists"* — and the Embermarket is that second area. With one bubble the market was either outside it (goblins spawning among the merchants) or the bubble had to swell wide enough to smother the wilds cells the region's whole encounter pressure lives in. ⚠️ The two authored bubbles **overlap** between `z = 14` and `z = 24` so there is no unprotected strip of road between the square and the market; `SafeZoneTests` pins every metre of it.
  - ⚠️ **`SafeZones.Set` replaces and must run before the per-cell `Add`s.** Otherwise a region transition leaves the previous realm's districts protecting empty ground in this one, and the symptom is enemies quietly refusing to spawn somewhere with nothing there. Same replace-never-merge rule as `ISaveable.Load`, same reason.
  - **Where the floors meet was arithmetic, not eyeballing.** The hub's 60 m floor ends at `z = 20`, so a 52 m floor centred at `z = 46` starts exactly there. A gap is a hole the player falls through; an overlap is two coplanar floors z-fighting along a seam. Neither is visible from the `.tres`, so the sum is written down beside it. `LoadRadius = 40` against a spawn at `z = 5` also means the district streams in as the player walks south rather than being resident from the main menu.
  - **A rule was written and deleted the same hour, and that is the entry's main finding.** "Every cell scene declares a `NavigationRegion3D`" looked obviously right and immediately reported four false failures: three Frostfang roosts *inherit* their `Nav` from `RoostCell`, which a text scan cannot see through, and the glacier legitimately has none because it is scenery. ⚠️ **A check that is wrong three times out of four is worse than no check** — it teaches authors to skim past the validator, which is the one habit that makes every other rule here worthless. What replaced it is narrower and true: the cell scene must *parse*.
  - **"The file exists" and "the file loads" are different claims.** The old rule was `ResourceLoader.Exists`, which a hand-authored `.tscn` with a syntax error passes; `GD.Load<PackedScene>` returning null is the real check, and loading does not instantiate so no component `_Ready` runs in the validator. ⚠️ It does **not** catch a missing `ext_resource` — Godot tolerates those and loads the scene anyway — and the retrospective says so rather than overclaiming what the gate covers.
  - **`TravelNodeComponent.TravelName` goes through `Loc.T` now**, closing a §6 violation that had been sitting in `town_hub.tscn` since Phase 25 for the price of one property. No migration was needed: `Loc.T` on a plain string returns it unchanged, so the old waystone's authored English still renders while new nodes author a key.
  - Two things worth carrying into 38L:
    1. **A validator that cries wolf is a validator nobody reads.** Both rules cut this session were cut for being *noisy*, not for being wrong in principle. Prefer the narrow true rule over the broad nearly-true one.
    2. **Verify a negative test twice before believing an engine quirk.** A `LoadRadius = 0.0` case appeared not to fire, which looked like Godot skipping a default-valued export — a finding that would have gone into §7 as a repo-wide authoring trap. Re-run cleanly, it fired: the first result was an artifact of the test harness, not the engine. An hour of "interesting" engine behaviour is usually your own shell.
  - Build clean + **1010** tests + `--validate` exit 0, with all four new rules negative-tested in both directions and the market scene's parse proven by the new load gate. ⚠️ **No `--play` walk** — code-only verification at the maintainer's standing request, so the cell's navmesh bake, its lighting after dark and the waystone attunement are *reviewed against the Godot 4.7 C# API*, not observed.
  - **Reachable when next played:** walk south from the town square — the district streams in at about `z = 6`; attune at the Embermarket waystone and it joins the map screen's jump list.
- [x] **38K, second pass — dressing the Embermarket** `[C/F]` ✅ *(maintainer-requested rework before 38L: "the market is very important and the current version is bland and stale")*
  - **Done when:** the district reads as the commercial heart of the Ember Crown rather than as a correct empty cell — without taking any of 38L's work.
  - **Landed:** **17 CC0 models adapted out of `assets/library/`** (market stands ×2, cart, barrel, sacks, hay, bench, well, bell tower, gazebo, cauldron, fence, tree, bush; three townsfolk rigs), a rewritten `embermarket.tscn` (24 prop instances → ~90, 7 models → 19, **607 nodes**), `data/dialogue/MarketBoard.tres` + 12 locale keys, `src/Audio/AmbientEmitterComponent.cs` + two `sfx.ambient.*` cues, and `tools/market_shots.gd` — a throwaway-turned-keeper harness that instantiates a cell in the real engine and writes PNGs, which is the only way anyone in this repo has ever *looked* at a cell without walking to it.
  - **The first pass was bland structurally, not by taste, and that is the finding worth carrying.** Six identical tents on six identical crates in two mirrored rows, six byte-identical lamps, four houses as a mirrored pair-of-pairs, one flat 52 m box, **two** authored materials and **zero** emission. Every one of those was individually defensible and the sum was a diagram of a market. **A cell built by copying one block six times will always read as a level**; variety has to be authored per instance or it does not exist.
  - ⚠️ **`StallW1..StallE3` did not move, and that was the constraint the whole rebuild was designed around.** 38K promised 38L a transform to copy; a per-stall yaw "for variety" would have silently invalidated every `Counter` offset. Variety came from a different **model** and different dressing per stall instead. The four extra stalls are named **`StallX1..X4` and carry no `Counter`** — the naming *is* the contract, so nobody reading quickly can mistake dressing for a merchant mount.
  - ⚠️ **NO RAISED GROUND ANYWHERE, and this cost a redesign.** The plan had a 0.3 m plaza dais. Godot's `CharacterBody3D` has **no step-up**: `MoveAndSlide` climbs nothing above `floor_snap_length` (0.1 m, never raised here), so a 0.3 m kerb is an invisible wall — while the navmesh's `agent_max_climb = 0.5` cheerfully bakes NPCs a path onto ground the player cannot follow them onto. **Verticality in this engine comes from props, never from terrain**: an 11 m bell tower, 8 m trees, 3.6 m gazebos. Every ground slab here is a 5–6 cm decorative skin with no collider.
  - ⚠️ **Nothing may stand in the aisle.** The first draft put the market's own banner dead centre at `(0, 0, -21)` — a 1.9 × 3.4 m sign, colliderless, in the middle of the only route in. It looked like a gate and behaved like fog. Both threshold banners are now at `x = ±4.3`, outside the 7 m spine. A prop with no collider on a walking route is worse than one with a collider; the player is told it is solid and then told it is not.
  - ⚠️ **`CLEAR_KEEP_TRANSFORM`, never `matrix_world = Identity`** (`CREDITS.md` has the long version). The `medieval_village` glTFs carry scale on a **parent node**, so unparenting by zeroing the world matrix exported all twelve props at ~1/200 scale — a 2.6 m stall written as 13 mm, with nothing logged. It was caught only because every file was **verified by parsing the written GLB**, which is now the rule for props as well as rigs.
  - ⚠️ **Superseded by 38L: the three townsfolk are no longer in the cell.** Twelve merchants answered "no people in 2,704 m²" better than three bystanders did, and one of the three wore `npc_townsman`, which turned out to be a hi-vis vest and a hard hat. The paragraph below is kept because its *finding* still stands.
  - **The three townsfolk were adapted by not adapting them.** All three already stood at 1.80–1.91 m with feet on the origin, and `npc_hooded` carries a bone-parented `Sword` that a Blender round-trip destroys (third-round finding, `CREDITS.md`). **When a rig needs no change, the correct adaptation is a file copy.** They also carry **no interactable and no `ScheduleComponent`** — the free interactable slot is what lets 38L promote any of them to a merchant with one component, and a schedule's `Destination` is a raw *world* `Vector3` against a cell authored at its own origin (the 37C bug in a different hat) that would then need hand-matching to 38L's shop hours, which nothing validates.
  - **The market's sound is an `sfx.*` emitter, not an `amb.*` bed, and the reason generalises.** `AmbienceSelection.Resolve` picks one **global 2D** bed from weather → in-town → clock, and "in town" is a `SafeZones` membership test that cannot tell the market from the square. A new member there would have changed the ambience of the whole town to get a sound into one district. `AudioCueRouting` already makes any `sfx.*` id positional on the SFX bus and `AudioDirector` already pools the player, so the emitter needed **no routing work at all** — the same "the prefix is the wiring" property Phase 31A bought and nothing had spent since.
  - **No new `ContentValidator` rules, deliberately** — 38K's own "a validator that cries wolf is a validator nobody reads" applies, and everything added is either already covered (the cell-scene parse gate; `MarketBoard.tres` and its keys ride the existing dialogue-graph and locale audits for free) or lives in a `.tscn`, which the validator does not scan.
  - **The notice board carries no `Effect` on any choice.** A board that started a quest would put market gossip in the journal — the failure 38Q's brief names, arriving eleven sub-phases early. It also turns the empty stalls into deliberate fiction ("every stall on this square stands let and unclaimed") instead of an unfinished level.
  - Two things worth carrying into 38L:
    1. **Look at the thing.** Every fault above — the camping tents in a market, the banner in the aisle, the empty middle, the guessed colliders — was invisible in the `.tscn` and obvious in the first render. A 60-line `SceneTree` script that instantiates a cell, lights it, and saves PNGs found more in one run than re-reading the file three times did. It is checked in at `tools/market_shots.gd`; point it at the next cell.
    2. **The stall models face their own local +X.** The west row therefore needs no rotation and the east row keeps the 180° yaw 38K already authored — so a 38L merchant standing at a `Counter` faces the aisle by standing at local `(2.2, 0, 0)` and looking back along −X. That fact is worth an hour if you have to rediscover it.
  - Build clean + **1022** tests + `--validate` exit 0 + the cell instantiated in-engine (607 nodes, no errors) and rendered at noon and dusk from six camera positions. ⚠️ **Still no in-game `--play` walk** — the MCP cannot inject input, so the navmesh bake at stream-in, the interact prompt on the notice board and the villagers' idle animation are *reviewed against the Godot 4.7 C# API and verified in a standalone instantiation*, not observed in a session.
  - **Reachable when next played:** walk south from the square; the braziers and the bell tower are visible from the seam. `E` on the notice board opens `dialogue.market_board`. `travel goto travel.ember_crown.embermarket` jumps there once attuned.
- [x] **38L — The Embermarket roster** `[C]` ✅
  - **Done when:** ~12 specialist merchants are authored, placed, and reachable by conversation.
  - **Landed:** **23 items and 5 new trade tags**, **12 shops** (`shop.embermarket.*`), **10 conversations** + 122 locale rows, **10 routines**, two merchant bodies, `ScheduleResource.Origin`, and twelve mounts in `embermarket.tscn`. One new leveled pool. **No new validator rules and no new systems** — the 38A–38J battery was built for exactly this drop and it held.
  - ⚠️ **38L is two jobs, and the second one is only possible because of the first.** The game had **26 items**, and `weapon`, `gem`, `jewelry`, `potion`, `herb`, `ore`, `trophy` and `relic` had **exactly one member each**. Twelve specialists carved out of that would have been twelve merchants selling each other's iron ingot, and 38F's whole premise — that `AcceptedTags`/`Specialties` make a merchant *mean* something — would have been decorative. The catalogue came first. **The general lesson: a system that prices identity cannot be exercised by content that has no identity to price**, and 38F/38H/38I have all been running against a shelf too thin to show what they do.
  - **`TradeTags.cs` kept its own bargain.** The file said a tag with no members is "a promise rather than a feature", and named eight. Five arrive here with goods wearing them the same day — `food`, `fish`, `textile`, `tome`, `fuel`. `contraband` waits for 38O's fence; `map` and `livestock` wait for anything at all to carry them. **A tag opened without an item is the thing that file exists to prevent.**
  - ⚠️ **A TRAVELLING MERCHANT CANNOT USE THE DIALOGUE ROUTE, AND THIS IS THE SUB-PHASE'S BEST FINDING.** `DialogueSession` evaluates `ShopOpen`/`ShopClosed` through `ShopHours.IsOpenAt` alone — **hours, never `IsInTown`** — and the only thing that hides an away merchant and zeroes their collider is `VendorComponent.ApplyPresence`. Wire a traveller through a conversation and they stand at their cart every day of the cycle with a working trade line: 38J's mechanic inverted, and nothing reports it. So the ten residents talk and **Sera and Quill carry `VendorComponent`s**, which is Hesk's route for Hesk's reason. The price is theirs too: those two `ShopId`s live in a `.tscn` and `--validate` cannot read them. Written into `CLAUDE.md` §8 as a rule rather than left as a discovery.
  - **`ScheduleResource.Origin` cost one export and removed thirty chances at a silent bug.** `ScheduleEntry.Destination` is a raw **world** `Vector3`, but a cell is authored at its own origin and streamed to its `Center` — so a stall position read out of `embermarket.tscn` lands 46 m north of where it was read. That is 37C's placement bug in a different hat. `Origin` defaults to `Vector3.Zero`, which is *exactly* the pre-38L behaviour, so the nine existing routines needed no migration — the same "the default is the ungated case" trick 38I's three gates play. ⚠️ The arithmetic lives in `ScheduleMath.Destination`, **not** on the resource, because the test project **crashes with an `AccessViolationException` constructing any Godot `Resource`**; a `Vector3` is a plain struct and passes fine. That is the constraint `ShopStock` was carved out for, met again from the other direction.
  - ⚠️ **`npc_townsman` is a hi-vis vest and a hard hat**, and it shipped in 38K as a market villager. It survived because nobody stood three metres from it; 38L found it in the first close-range render, dropped it from the roster and retired the villager wearing it. **Look at the thing, at eye level, from close** — 38K's own lesson, re-earned within one sub-phase of being written down.
  - ⚠️ **The library is out of medieval bodies and the entry in `CREDITS.md` says so rather than papering over it.** Seven usable bodies dress twelve merchants, so four appear twice. The five unused women are **CC-BY 3.0** (licence, not taste — the Witch and the Worker would have been perfect) and the four unused men are a business suit, a hoodie, jeans-and-sneakers and an astronaut. All three modern ones were rendered and rejected on sight. Closing it needs an open-web pull of Quaternius's own CC0 **RPG Character Pack**, which 38L did not make.
  - **The three 38K villagers are gone**, and that is a deletion rather than a loss: they were authored for one stated reason — "no people in 2,704 m²" — and twelve merchants answer it better than three bystanders. One of the three was the hi-vis worker.
  - **The roster is a market rather than twelve shops**, and the tag pairs are what do it: Hana's fish oil is `fish` **and** `reagent`, Ash's pitch is `fuel` **and** `reagent`, Corvin and Hana both stock the smoked fish. A specialist sealed off from every other specialist is a menu entry, not a merchant.
  - Two things worth carrying into 38M:
    1. **The validator battery is the deliverable, not the shops.** Twelve shops, ten conversations and 23 items landed with **zero new rules** and the only failures were caught by rules 38A–38J had already written — including the two that fired in the negative pass for reasons *other* than the one under test (an investment gate on a shop with no ladder trips "needs 1 rung, sells 0" first). ⚠️ **A rule can only be negative-tested by a break that actually reaches it**; a break that trips an earlier rule proves nothing about the one you meant to test, and both of mine did that on the first attempt.
    2. **Generate the authoring, hand-write the prose.** All 55 `.tres` files came out of throwaway generators, which is what made a 12-shop drop tractable at all — but every merchant's four lines of dialogue and every file's rationale comment were written by hand, because a generated justification is a comment nobody will trust in six months.
  - Build clean + **1105** tests + `--validate` exit 0, with **five rules negative-tested in both directions** (traveller-only consumable, specialty outside `AcceptedTags`, an ungated trade choice, a shop gating every row, a spread too thin for the premium) — each firing its own message on broken data and silent on the authored data, with every touched file backed up and restored rather than reverted through git. The cell instantiated in-engine (1 063 nodes, no errors) and rendered at noon and dusk from nine camera positions. ⚠️ **No in-game `--play` walk** — the MCP cannot inject input, so the merchants' idle animation, the interact prompts and the schedules walking them to their stalls at opening are *reviewed against the Godot 4.7 C# API and verified in a standalone instantiation*, not observed in a session.
  - **Reachable when next played:** walk south and talk to anyone; every stall has somebody behind it. `shop shop.embermarket.jeweller` opens Nessa's case from the console and names any hours override; `time 26` rolls the day until Sera or Quill is in town; `rep faction.villagers 60` opens Tam's Treatise on Wardings; `shop invest shop.embermarket.ironmonger` buys Gilda's rung and unlocks her steel sword.
- [x] **38M — Crossway Post: the toll, the permit and the bribe** `[F/C]` ✅ *(maintainer call before the session: charge portal crossings only, and split the district out — mechanic now, cell in 38M2)*
  - **Done when:** a toll gate charges on the single path every crossing converges on, a permit exempts, and a bribe costs standing.
  - **Landed:** `TollFee` + `TollOutcome` (pure, 4 tests), three exports on `RegionResource`, `GameBootstrap.PayToll`, a toll-aware portal prompt, `ServiceKind.Passage` with `GrantedFlagId`/`ReputationDelta`, **two services**, **two NPCs** flanking the Ember Crown portal, 9 locale rows and **three validator rules**. One new C# file.
  - **"The single path every crossing converges on" is a function, not a place.** The charge sits in `GameBootstrap.OnRegionTransitionRequested`, which the portal's event *and* the `region` dev command both arrive at — gating `RegionTransitionComponent` alone would have left the console a free ride, which is exactly the mistake 38C caught on the travel fee (the map button and `travel goto` converge, so the fee went where they meet). The Crossway Post *cell* is 38M2; what shipped today is the toll, and it is charged whether or not there is yet a gatehouse to look at.
  - ⚠️ **Fast travel is deliberately untolled.** A cross-region jump already pays `TravelFee.CrossRegionFee` (40), and charging a toll on top is two charges for one journey — the toll-booth feel 38C designed *against*. The consequence is intended and is the interesting part: at 25 gold the road undercuts the map, so the wardens are now competing with fast travel rather than taxing it.
  - **A permit and a bribe are the same verb, and that is why this cost one enum member.** `ServiceKind.Passage` + one branch on 38D's `ServiceComponent` inherited the price, the standing discount, the hostile refusal, the charge-before-the-verb ordering and the whole prompt battery. The two differ in **three numbers**. A `TollGateComponent` would have re-implemented all of it.
  - ⚠️ **`UnlockFlagId` doubles as the receipt, so a bribe cannot use it** — recorded there, `AlreadyHeld` refuses to sell a second one, and a bribe you can only pay once *is* a permit. Hence `GrantedFlagId`, which the crossing **consumes**. The pass is deliberately not permanent: a permanent pass sold under the permit's price is strictly better than the permit, and the toll stops being a sink after one bribe.
  - **The bribe's standing cost needed no new currency, because 38C already built the machine.** −8 against villagers who start at 10 means three bribes drop the player below Neutral, and every merchant in the Ember Crown then prices off that. The cheap crossing is charged twice — ten gold at the gate, and a little more at every counter in town, forever.
  - ⚠️ **Two NPCs, not one with two services.** `GetComponent<T>` returns the first child match (38E), so a permit and a bribe on one body leaves the second silent. They are also dressed to be told apart at a glance — the guild body every official in the square wears, and the hood.
  - **Three rules, negative-tested both ways**, and one of them re-earned 38L's warning: breaking the bribe's `GrantedFlagId` to test "a passage that grants nothing" *also* trips the region-reachability rule, because the flag the region names stops being granted. Both messages fire, which is the right outcome — but the second one is not evidence about the first, and it would have been easy to read it as such.
  - **Story flags have no database, so the reachability rule is the only thing between a typo and an uncrossable road.** `--validate` now checks that each flag a tolled region names is granted by some authored `Passage` service, as a union — 38D's recipe-reachability shape. Placing the warden proves nothing: `.tscn` is still not scanned.
  - Two things worth carrying into 38M2:
    1. **A throwaway render harness is still cheaper than reading the file.** `tools/market_shots.gd` copied to a scratch file with four camera positions found both new NPCs standing correctly at eye level in one run. It was deleted afterwards — the harness is the *technique*, not a second committed tool.
    2. **The toll is authored on both regions and nothing enforces that they agree.** One gate, one road, two `RegionResource` blocks, and the reachability rule only catches a flag that nothing grants — not a `TollGold` of 25 northbound and 250 southbound. If 38M2 gives the Crossway its own cell, that is the moment to ask whether the toll should live on the *cell* instead.
  - Build clean + **1109** tests + `--validate` exit 0 (four break paths tested, each firing its own message, every file restored from a backup rather than through git) + the town hub instantiated in-engine (701 nodes, no errors) and rendered at noon and dusk from four positions + a clean `--play` boot with the new region fields loading. ⚠️ **No in-game walk** — the MCP cannot inject input, so the gate prompt's four states, the two purchases and the pass being spent at the crossing are *reviewed against the Godot 4.7 C# API*, not observed in a session.
  - **Reachable when next played:** the warden and the gate-hand stand either side of the Frostfang portal, four metres from where you spawn. Buy the permit (250) or the quiet word (10, and eight standing), then walk through; the portal's prompt names the price, your papers or your shortfall before you press `E`. `region region.frostfang_reach` in the F1 console goes through the same charge.
- [x] **38M2 — The Crossway Post itself** `[C]` ✅ *(plus two maintainer-requested engine changes, below)*
  - **Done when:** the Crossway Post is a cell — a gatehouse on the road, wardens, a caravan yard — and the toll, the permit and the bribe are bought *there* rather than in the town square.
  - **Landed:** `crossway_post.tscn` (300 nodes) at `Center (0, 0, -116)`, three adopted models, `RegionResource.PortalPoint`, the two wardens moved out of `town_hub.tscn`, a travel node and its locale row.
  - **The portal moved, and that is the half of this that made the toll mean anything.** 38M charged for a road whose crossing happened four metres from the player's bed. `PortalPoint` (`Vector3.Zero` = the old spawn-relative placement, so Frostfang needed no edit — the same "default is the ungated case" trick 38I and 38M both used) puts the Ember Crown's door at world `z = -122`, the far side of the gate. The wardens are now people you walk past. ⚠️ **One point per region**, so a region with two neighbours would stack both portals on it; two regions exist, and this becomes per-neighbour when a third does.
  - ⚠️ **THE `rts` PACK IS ROUGHLY 1/6 SCALE AND NOTHING IN THE FILES SAYS SO.** A "Wooden Fortress Gate" measures **1.94 × 0.67 × 0.31 m** out of the box — a gateway two feet high. It was caught by measuring every candidate against a 1.8 m reference box *before* authoring anything around it, which took one throwaway harness run. Adopting one of these blind would have produced a cell whose every transform was wrong and whose wrongness only showed at eye level. The pack is authored for a top-down camera; **assume the same of any pack whose models were made for a different camera.**
  - **The adaptation is `nodes/root_scale` in the `.import`, not Blender.** One number in one file, no round-trip (the thing that destroyed bone-parented parts in the third-round audit), and no per-scene scale for future placements to guess at. Recorded in `CREDITS.md` with the raw sizes.
  - ⚠️ **`prp_gate_palisade` has no opening in it** — it is a closed palisade, so the gate is a **gap between two of them**: each is 15.93 m wide at `x = ±11.9`, inner edges at `x = ±3.94`, and the 8 m road runs through the 7.88 m gap. Nothing has to animate for the player to pass, which is the cheapest possible gate.
  - **The library search paid for itself in the negative, too**: `castle_gate` measured identically to the fortress gate (same mesh, different name), and `path_straight` was dropped in favour of a `BoxMesh` road skin — a road out of tiles is dozens of nodes for something one box does.
  - Two things worth carrying into 38N:
    1. **Measure before you place, not after.** Every trap in this sub-phase was a number: the pack's scale, the palisade's width, the seam at `z = -90`. Each was cheap to check and expensive to discover from a screenshot.
    2. **A cell is now ~300 nodes and always resident.** With distance streaming gone, every cell added to a region is permanently in the tree — 38N adds two settlements, and that is the first time the residency decision will actually be worth measuring.
- [x] **Engine changes landed alongside 38M2** *(maintainer direction, not a sub-phase)*
  - **Regions load whole.** `RegionStreamer` instances every cell of the active region on entry and never unloads during play; `StreamDecision`, its 75 lines of hysteresis tests, `UnloadMargin` and `RegionCellResource.LoadRadius` were all deleted with the rule, along with the validator's `LoadRadius <= 0` check and ten authored lines. `IsSettledAround(origin)` became `IsSettled()`.
    - ⚠️ **Both regions cannot be resident at once, and it is a world-layout problem rather than a streaming one.** Frostfang's `dragon_roost` (25, 0, -20) and `ancient_aerie` (25, 0, -110) share coordinate space with the Ember Crown's `arena` (55, 0, -10) and `wilds_north` (0, 0, -65). Loading both would put two regions inside each other. Phase 44 owns that.
    - **The save path was the thing worth checking and it needed no change.** `CellPersistenceDirector` snapshots on `RegionCellUnloadedEvent` because "the base `SaveManager` only restores actors alive at load time". Resident cells mean those actors are *always* alive at save time, so the normal path covers them and the ledger is now only exercised across a region transition, where `UnloadAll` still fires the event.
  - **Spawn caps to 15** *(reverted to 5 in 38N1 by maintainer direction — fifteen read as too much pressure against a whole-region residency; the widened `SpawnRadius` stayed)*. `EncounterDirector.MaxConcurrent` 5 → 15 and the sandbox goblin camp 3 → 15. ⚠️ `EnemySpawnDirector._Ready` seeds its whole population in one go, so `SpawnRadius` had to go 6 → 14 with it — fifteen bodies in the old circle spawn inside one another, which is a defect that would have read as a physics bug.
- [x] **38N1 — Emberdeep Mine** `[C]` ✅ *(38N split in two at the maintainer's call: one settlement per session, the way 38K/38L were)*
  - **Done when:** the first production settlement exists and a dev command can print the realm's best arbitrage.
  - **Landed:** `emberdeep_mine.tscn` (396 nodes) at `Center (99, 0, -10)`, **2 items**, **2 shops**, 2 conversations + 27 locale rows, 2 routines, four adopted models, and `EconomyReport` behind both an `economy` console command and a new **`--economy` headless flag**.
  - ⚠️ **ARBITRAGE IS MATHEMATICALLY IMPOSSIBLE TODAY, AND THE TABLE PROVES IT RATHER THAN ASSERTING IT.** `ShopPricing.BuyPrice` clamps its markup to `>= 1` and `SellPrice` clamps its fraction to `0..1`, so `sell <= value <= buy` holds at **every** shop by construction (38A) — which means a carry between two merchants is *always* a loss, whatever the content says. 48 goods across 18 shops, every margin negative, best route −2. **The report says so in its own output.** This is the strongest possible argument for 38G: regional demand moves an item's *value* per settlement, and that is the only thing that can turn these positive. 38N was written as the content that makes the demand table true; it turns out to also be the proof that the table is not optional.
  - **A production settlement is a SOURCE and a SINK, not more stalls.** Bregan Holt sells ore at `BuyMarkup 1.15` (the realm's lowest — the Embermarket runs 1.4–1.65) and barely buys; Marta Quill pays `SellFraction 0.62` with `food` as her specialty (the realm's most generous) and sells almost nothing. ⚠️ **Her `AcceptedTags` deliberately exclude `ore`.** The temptation was to let her buy everything so the walk is never wasted, and that is exactly the decision that would flatten two settlements back into one. It shows immediately: the mine is now the cheapest seller of every ore and the best buyer of every food in the game.
  - ⚠️ **`npc_merchant_f` IS MODERN DRESS — white t-shirt, tan trousers, blue trainers — AND IT SHIPPED IN 38L.** Found by rendering it at eye level for Marta. 38L's own retrospective says the modern bodies "were rendered and rejected on sight"; this one was among the seven *adopted*, so it never got that look. Marta wears `npc_townswoman` instead, and the defect is recorded in `CREDITS.md` under "Known, not fixed" rather than propagated to a third district. **This is the third time the same trap has fired** (hi-vis worker 38K→38L, this now): a body is only vetted at the range someone actually rendered it.
  - ⚠️ **The mine head shipped facing backwards in the first render** — the adit pointed into the rock and the yard saw a blank stone face. A yaw derived by reasoning about which way a model's +Z points was wrong, and one render settled it in a minute. **Do not reason about model facing; rotate it, render it, look.**
  - **The cell needed a second pass in the same session**, and that is 38K's lesson arriving early enough to be cheap: the first draft was a cluster of props in the middle of 2,704 m² of dark ground. 22 more props — ore heaps into the workings, pines and rock closing the verges, a third brazier — took it from a diorama to a site. No new art, just density.
  - **`--economy` exists because the F1 console cannot be driven from this environment.** A report reachable only through the console would have shipped having never once been run — the `CraftingComponent.Learn` shape. Both entry points call one function, so the table the maintainer reads and the table a tool captures cannot drift. It always exits 0: an observation, not a gate.
  - Two things worth carrying into 38N2:
    1. **Author the sink's refusals first.** What a settlement will *not* buy is what makes it a different place; the accepted-tag list is the design, and the shelf is decoration on top of it.
    2. **Every `rts` model is ~1/6 scale, now confirmed twice.** Raw heights here were 1.17 m, 0.39 m, 0.42 m and 0.25 m. Measure against a 1.8 m box before authoring anything around it; adapt with `nodes/root_scale`.
  - Build clean + **1116** tests + `--validate` exit 0 + `--economy` captured + the cell instantiated in-engine (396 nodes, no errors) and rendered at noon and dusk from seven positions + a `--play` boot with **all seven** Ember Crown cells resident. ⚠️ **No in-game walk** — the conversations, the interact prompts and the two routines are *reviewed against the Godot 4.7 C# API*, not observed in a session.
  - **Reachable when next played:** `travel goto travel.ember_crown.emberdeep_mine`, or walk east past the arena. Talk to Bregan for ore at a price nobody else in the realm asks; take him bread and he will barely look at it. Take the bread to Marta instead. `economy arbitrage` at F1 prints the whole picture.
- [x] **38N2 — Tarn's Landing** `[C]` ✅ *(plus the catalogue pass and the body pull, at the maintainer's call)*
  - **Done when:** the fishing hamlet exists as a cell with its own source/sink pair, and the realm has three markets to price against.
  - **Landed:** `tarn_landing.tscn` (350 nodes) at `Center (-106, 0, -10)`, **7 items**, **2 shops**, 2 conversations + 27 locale rows, 2 routines, **5 adopted models**, and Hana and Sera re-dressed out of modern clothes.
  - ⚠️ **THE OPEN-WEB PULL RETURNED A FILE THAT WAS ALREADY ON DISK, AND THAT IS THE SUB-PHASE'S BEST FINDING.** 38L declared the library "out of medieval character bodies" and named Quaternius's CC0 RPG Characters pack as the fix. The pull produced an **Adventurer byte-identical to `assets/library/women/adventurer.glb`** — `md5 cba6105c…`, vendored since the 38L migration, **never adapted**, and the best body of the whole search. 38L's "the five unused women are CC-BY 3.0" was simply wrong about that one. **The shortage was a bookkeeping error.** Before pulling anything, `ls` what is already vendored and check the licence field rather than a sentence in a retrospective.
  - ⚠️ **Four of six candidates were rejected on sight, and the sixth had a chainsaw.** `Woman` is a black-and-pink top and blue jeans; `Lis` is a cartoon punk with a blue mohawk holding a chainsaw; `Monk` is not a person at all (an ornament with coins and rays); `Wizard` has four bones. One render pass, front and back, disposed of all four. **A 4-in-6 rejection rate is the entire argument for the gate**, and it is now the third sub-phase running to prove it.
  - **`npc_merchant_f` is retired from the game.** Hana wears the dress body, Sera the adventurer's kit. The defect it caused took three sub-phases to travel: shipped 38L, found 38N1, fixed here.
  - **The water is a decal and it needed a lakebed to be visible.** The first pass was a translucent plane over the *same shingle the shore is made of*, and at eye level it read as flat grey ground: **a transparent surface needs something dark underneath it to be transparent against.** A near-black skin under the tarn (no collider) fixed it in one edit, and the waterline now reads as an edge.
  - ⚠️ **The dock and jetty models are built to stand IN water** — posts below their own origin (`min.y` −2.35 / −1.05). The jetties are sunk 0.30 m so their decks land flush instead of as a 34 cm kerb, which with no step-up (38K) would have been an invisible wall on the one route to the water.
  - **The third market makes the table a network rather than a list.** `--economy` now shows fish cheapest at the Landing and best-paid at the mine's company store, and copper ore cheapest at the mine and best-paid at the Landing's chandler — two settlements that each want what the other digs up, with the Embermarket in the middle. 55 goods across 20 shops. Every margin is still negative and will be until 38G.
  - **The sink's refusal is now doing visible work.** Odger accepts `textile`, `metal`, `fuel` and **not `fish`** — and says so in his own dialogue before the player can ask, which is the difference between a refusal and a greyed-out row.
  - **A crate scare that was not a defect**, worth recording because the check is cheap: the crates read as 3 m cubes in one render. Measured — `prp_crate` is 1.24 m and `prp_barrel` 0.85 m, exactly as authored, and the render was foreshortening on a stacked pair six metres from the lens. **Measure before believing a screenshot, in both directions.**
  - Two things worth carrying into 38O:
    1. **Check the library before the web.** This session spent its first hour pulling an asset it already owned.
    2. **Budget the second dressing pass into the estimate.** Three cells in a row have needed one, so it is not a surprise any more — it is the second half of the job.
  - Build clean + **1132** tests + `--validate` exit 0 + `--economy` captured + the cell instantiated in-engine (350 nodes, no errors) and rendered at noon and dusk from eight positions + a `--play` boot with **all eight** Ember Crown cells resident. ⚠️ **No in-game walk** — conversations, prompts and routines are *reviewed against the Godot 4.7 C# API*, not observed.
  - **Reachable when next played:** `travel goto travel.ember_crown.tarn_landing`, or walk west past the wilds. Buy a fresh catch off Wenna for four gold and carry it to Marta at the mine. Try to sell Odger a fish and watch him point you back up the beach.

- [x] **38O — Hollowreach: contraband and fences** `[F/C]` ✅ *(mechanic and cell in one session, at the maintainer's call)*
  - **Done when:** contraband-tagged goods sell only at the wharf, at a two-sided reputation cost, and confiscation is recoverable.
  - **Landed:** `contraband` opened in `TradeTags` with **5 goods** wearing it the same day, one branch in `Accepts`, four exports on `ShopResource`, `ContrabandLaw` + `ContrabandImpound` (2 new C# files), `ServiceKind.Search`/`Redeem` and **2 services**, **2 fence shops**, `hollowreach.tscn` (585 nodes) at `Center (-106, 0, 74)`, 2 conversations + 30 locale rows, 2 routines, **4 new NPC placements** and **6 validator rules**. **No new art at all.**
  - ⚠️ **`contraband` IS THE ONE TAG THAT FAILS CLOSED, AND THAT INVERSION IS THE WHOLE SUB-PHASE.** 38F's load-bearing decision was that *both empties mean yes* — an empty `AcceptedTags` is how a general store is authored. Under that rule Aldreth fences smuggled goods across the most respectable counter in town, and no amount of authoring fixes it, because the gap is in the semantics rather than in the data. So contraband **dominates**: an item wearing it is refused by every shop that does not name `contraband` itself, whatever else either list says. **It is one branch at the top of `TradeTags.Accepts` and nowhere else** — the vendor window, the sale itself and `EconomyReport`'s arbitrage table all already routed through that one function, so the prohibition reached three call sites for the price of one. A `ShopResource.IsFence` flag would have been a second authority over the same question.
  - **The catalogue came before the merchants, again, and 38L's lesson generalises further than it was written.** 38L said a system that prices identity cannot be exercised by content with no identity. 38O's version is narrower and sharper: **a tag that means “nobody will take this” is meaningless until something takes it and something else refuses it.** Each of the five goods therefore wears `contraband` *and* an ordinary trade — a stolen signet is `jewelry` and Nessa still turns it down — so the dominance rule is exercised by the authored data and not only by a test.
  - ⚠️ **The two-sided cost is PER SALE, and that is deliberately the opposite of 38H's per-unit ruling.** 38H made a stack decay across its own units so selling tidily was not punished. Standing is not divisible the same way: charged per unit, one click on a stack of twenty moves the player three reputation tiers. The two are only in tension if 38H is read as a rule about stacks rather than about appetite. Applied in `VendorPanel.Sell` **beside `Absorb`, after the goods change hands** — every early return above it leaves the player still holding the item, and a reputation change is louder than a glutted shelf because it toasts and reprices the whole region.
  - ⚠️ **A FENCE CANNOT AUTHOR THE FACTION SHE ANSWERS TO, AND THIS WAS FOUND BY READING THE NUMBERS RATHER THAN BY PLAYING.** `faction.outlaws` starts at **−30** — tier `Hostile`, at or below its own `HostileThreshold` of 2 — so a shop with `FactionId = "faction.outlaws"` is hidden by `VendorComponent.ApplyPresence` and refuses to trade from the first minute of a new game, forever, because the only way to raise outlaw standing is to trade with her. Both fences author an **empty** `FactionId` and price flat. **The standing a merchant MOVES and the standing she PRICES BY are two different questions**, and nothing before 38O had to separate them.
  - **The outlaw half of the cost needed no machinery, which is the best thing in the sub-phase.** `EnemyAIComponent.PlayerIsTarget` already reads `IsHostile(faction.outlaws)`, so six fenced sales carry the player from −30 to Neutral and **the bandits on the road stop attacking on sight**. Nothing was written to make that happen. The villagers' half is 38M's bribe exactly: standing charged again at every honest counter through 38C's ramp, forever.
  - **Confiscation is a service, not a component, and the search is priced 0 on purpose.** `Search` and `Redeem` inherit the price, the standing discount, the hostile refusal, the charge-before-the-verb ordering and the whole prompt battery from 38D. ⚠️ **A search the player can be too poor to undergo waves the contraband through** — `ServiceRules` refuses anything unaffordable before the verb runs, so a priced search is a validator error rather than a balance question. ⚠️ And `Redeem` is **the only service in the game whose price is not the number authored on it**: `PriceGold` is the per-unit fine and `ContrabandLaw.Fine` multiplies it by what is held, because a flat redemption fee makes one enormous seizure cheaper per unit than a small one and rewards carrying more.
  - ⚠️ **Two members rather than one, which is the opposite call to 38M's and this entry says so loudly.** The permit and the bribe were one `ServiceKind` because they are the same verb at three different numbers. `Search` and `Redeem` are not: one takes and one gives, their preconditions are inverses, and only one of them computes its own price. A single member would have needed a branch inside every shared function to tell them apart — the enum doing its job badly.
  - **Recoverability is enforced, not intended.** `--validate` fails a `Search` with no `Redeem` anywhere in the realm, as a union across services — 38M's toll-flag reachability shape. A permanent seizure is theft rather than a fine, and it is the one thing that would make carrying contraband a risk the player cannot price. Nothing about the search resource can see that on its own, which is why the rule lives outside it.
  - ⚠️ **A COLLIDER COPIED FROM ANOTHER CELL WAS WRONG, AND ONLY THE RENDER CAUGHT IT.** `wilds_north` gives `prp_ruin_pillar` a `(3.5, 0.7, 0.7)` box — a pillar lying on its side. It stands up here. Copying the number put three 4 m columns in the cell with 70 cm of collider and three metres of walk-through stone above them. **38M2's “measure before you place” now extends to numbers lifted from a sibling cell**: another cell's collider is evidence about that cell's placement, not about the model.
  - **No new art, and the library really is empty now.** Every model is one adopted for an earlier cell, re-read: the fishing hut is a harbourmaster's shed nobody mans, the ruin walls are a breakwater, the watch tower stands over a wharf it has stopped watching. 38N2 spent an hour pulling an asset already on disk; this session spent none, because `manifest.json`'s licence field was read first — and it confirmed the shortage is now real rather than a bookkeeping error. **Every CC0 body in `assets/library/` is already in `assets/models/characters/`**; the unadopted men are a business suit, a hoodie, jeans and an astronaut.
  - **The second dressing pass was budgeted and still needed.** 38N2 said three cells in a row had needed one so it is the second half of the job. The first render put the wharf in the middle of 2,704 m² of bare mud with the entire east half empty; 39 more props fixed it. **Fourth cell in a row. It is not a pass any more, it is a step.**
  - Build clean + **1158** tests + `--validate` exit 0 + `--economy` and `--state` captured + the cell instantiated in-engine (585 nodes, no errors) and rendered at noon and dusk from twelve positions + a `--play` boot with **all nine** Ember Crown cells resident. **Seven validator breaks tested across two runs, each on its own resource so every message was attributable to one rule** — the pairing rule, the completeness rule, an unknown faction, a backwards sign, a priced search, a free redemption, and a realm that seizes with nowhere to redeem. Every touched file was restored from a backup rather than through git.
  - ⚠️ **Two things were NOT verified.** The `F1` console needs a human and the MCP cannot inject input, so the interact prompts, the search and redeem presses, the two fence conversations and the routines are *reviewed against the Godot 4.7 C# API*, not observed in a session. And the arbitrage table's contraband rows sit below `--economy`'s fifteen-row cut, so **the report was not read back to confirm contraband is quoted only at the fences** — that claim rests on the unit tests and on `EconomyReport` calling the same `TradeTags.Accepts` the panel does.
  - **A pre-38O save warns and carries on**, which is correct and worth knowing before it is mistaken for a defect: `SaveManager` skips a saveable with no entry, so `--play` on an old slot logs *“no usable entry for 'contraband_impound'”* and the ledger keeps its (empty) state. Same shape as `ShopStockService`'s own note.
  - Two things worth carrying into 38P:
    1. **Break one rule per resource, or the run proves nothing.** 38L warned that a break tripping an earlier rule is not evidence about the rule under test; 38O's first attempt did exactly that (stripping the fence tag also tripped the dead-cost rule) and the fix was to spread the breaks across four different files so each message named a different id. Seven rules then took **two** validator runs instead of seven.
    2. ⚠️ **A stray `.tres` in the wrong `data/` folder hangs the whole engine, silently.** A too-broad `cp` during a negative-test restore put two `ServiceResource` files in `data/shops/`; `ShopDatabase` threw an `InvalidCastException` inside `GameBootstrap._Ready`, which never reached `quit()` — so `--validate` **hung for minutes** instead of failing. It reads exactly like an environment problem and it is not one. **When a headless run hangs, read the log before blaming the machine**: the exception was in there the whole time.
- [x] **38P — Consignment house** `[F/C]` ✅ *(38P split in two at the maintainer's call, the way 38M/38M2 and 38N1/38N2 were: consignment this session, the appraiser as 38P2)*
  - **Done when:** an item lists, and sells over in-game days above vendor price without the purse cap.
  - **Landed:** `ConsignmentRules` + `ConsignmentLedger` (2 new C# files), three exports and an `IsConsignment` on `ShopResource`, `ServiceKind.Collect` and its verb, a `Consign` branch in `VendorPanel`, **6 validator rules**, **1 shop**, **1 service**, 1 conversation + 1 schedule + 14 locale rows, and **10 props and 2 NPC placements in the Embermarket**. **No new art at all** — every model is one the cell already used.
  - ⚠️ **THE FEATURE IS THREE THINGS `Consign` DELIBERATELY DOES NOT CALL, AND THEY LOOK LIKE OVERSIGHTS BESIDE `Sell`.** No `TakePurse` (38C), no `Absorb` (38H), no `FenceStanding` (38O). A broker fronts no money, so there is no purse to spend before the goods change hands and no short payment to guard against; she never owns the item, so her appetite has not fallen; and contraband is refused by `TradeTags.Accepts` at any counter that does not name it, so she needs no fence branch. The result is the one thing the realm had no room for: **a stack of twenty lists for twenty times one**, and the game's best loot stops being its least sellable. Every purse in the realm is smaller than one rolled Legendary.
  - ⚠️ **AND IT STILL CANNOT INVERT THE SPREAD, BECAUSE IT ROUTES THROUGH `ShopPricing.SellPrice`.** `ConsignmentRules.Gross` is a one-line call to it, so the `0..1` fraction clamp holds any authored payout to the item's value, and `sell <= value <= buy` survives 38P untouched. **This was the whole reason to reuse that function rather than write the multiply again** — a second rounding rule for the same question is how two numbers drift, and the drift here would have been an infinite gold loop against the cheapest merchant in the realm. `ConsignmentRulesTests` asserts it at `fraction = 1`, `4` and `float.MaxValue`.
  - **Maturation is `ShopStock.IsRestockDue` under another name and calls it directly.** The question is identical — has *N* days passed since a stamped day — down to both edge cases that function learned the hard way: the `long` widening so a never-stamped `int.MinValue` does not overflow back to "not due", and a clock that has gone backwards counting as due rather than freezing for the run. A second day-arithmetic helper would have had to rediscover both, and the backwards-clock one is a quickload away.
  - **The ledger stamps the payout at listing time, which is strictly better than the impound it is copied from.** `ContrabandImpound` holds template ids and hands goods back plain, so an affixed instance would return stripped — recorded there as a known limit. Here the item is *sold* and never comes back, so pricing it while the instance still exists means a rolled Legendary consigns losslessly and no `ItemInstance` blob has to survive a second save path. **The same shape, one requirement lighter, and the lighter requirement is the one that removes the defect.**
  - ⚠️ **`Collect` IS A SEPARATE `ServiceKind`, AND THE SPLIT IS 38O's REASONING RATHER THAN 38M's.** Listing lives in the vendor window because the player must choose *which* item; collecting has nothing to choose, so it is a service and inherits the price, the standing discount, the hostile refusal, the charge ordering and the whole prompt battery from 38D for about ten lines. ⚠️ It is **free, and `--validate` enforces it** — not for tidiness: `ServiceRules` refuses any service the player cannot afford *before* the verb runs, so a priced counter would leave a player whose only money is on the shelf too poor to be paid. That is 38O's priced-search rule arriving from the opposite direction.
  - ⚠️ **TWO ENTITIES, BECAUSE AN ENTITY GETS ONE INTERACTABLE.** Mirelle has to carry a `DialogueComponent` (the Embermarket's rule: the ten residents talk, the two travellers carry a `VendorComponent`), so a `ServiceComponent` behind it would never fire. The clerk is therefore a second person — and the constraint reads better than it deserves: you hand goods to a broker and you collect coin from a clerk. ⚠️ **Halder has no schedule on purpose**: Mirelle keeps the shop's hours and walks off at six, and a clerk who did the same would turn a sale the player has already earned into a locked door.
  - ⚠️ **AN EXISTING RULE HAD TO LEARN ABOUT THE EXCEPTION, AND THE FIX BELONGED IN THE OLD RULE.** `--validate` failed on the first run with *"shop stocks nothing — its window would open empty"*, which is correct for twenty-two shops and exactly wrong for the twenty-third: a broker's left-hand panel is *supposed* to be empty. The exemption went into that rule rather than into a new one saying "unless", because two authorities on when an empty shelf is a defect is how the second one gets forgotten.
  - ⚠️ **EVERYTHING FACED THE WRONG WAY AND ONLY THE RENDER SAID SO — the fourth time this trap has fired.** The stall, the counter and both NPCs were authored at a yaw reasoned out from the neighbouring stalls' bases, and all four faced east into the buildings with their backs to the square. **The two east stalls disagree with each other** (`StallX2` is yaw 90, `StallX4` is 270), so there was no right answer to copy. One render settled it; a second caught Mirelle standing *in front of* her own counter in the customer's spot. 38N1 wrote "do not reason about model facing; rotate it, render it, look" — it applies to people and furniture, not only to a mine head.
  - **The second dressing pass was needed on a cell that was already dressed**, which is new. The first pass put two counters and three boxes on open ground and read as exactly that; a bench, a lamp and stores stacked behind turned it into somebody's premises. ⚠️ Then the lamp had to move *again* — it landed in a walking lane. **Fifth cell running. It is not a pass, it is a step, and now it is a step even on a finished cell.**
  - ⚠️ **`--economy` DOES NOT KNOW CONSIGNMENT EXISTS.** The arbitrage table is shop-to-shop only, so the realm's price report is now missing the realm's best payout (~0.70 of value against the most generous shop's 0.62). Left deliberately: 38P2 refactors `EconomyReport` to expose a shared `BestBuyer` for the appraiser, and doing that surgery twice is worse than a table with a named gap. **Recorded in `NOW.md` rather than left for someone to notice from a number.**
  - Build clean + **1198** tests + `--validate` exit 0 + `--economy` and `--state` captured + the premises rendered at noon and dusk from five positions across four passes + a `--play` boot with all nine cells resident, 27 objects restored and **0 project errors**. **All six new rules negative-tested both ways in two runs** — five broken at once on two files because each reads a different field, then the realm-wide payability rule alone with the service moved out of `data/` entirely. ⚠️ Note the fifth break also tripped the pre-existing purse-with-no-restock rule; both messages named themselves, so it stayed attributable, but it is 38O's masking trap in miniature.
  - ⚠️ **Two things were NOT verified.** The `F1` console needs a human and the MCP cannot inject input, so **the List button, the collect press, Mirelle's conversation and her schedule are *reviewed against the Godot 4.7 C# API*, not observed in a session.** And no save was taken *after* a listing, so the ledger's round trip through `Save`/`Load` is covered by its shape (copied from `ContrabandImpound`, cleared before rebuild) and not by a reload.
  - **A pre-38P save warns and carries on**, as 38O's did: `--play` on an old slot logs *"no usable entry for 'consignment'"* and the ledger keeps its empty state. Correct, and worth knowing before it is mistaken for a defect.
  - Two things worth carrying into 38P2:
    1. **Reuse the pricing function, do not re-derive the price.** Every invariant this sub-phase did not have to defend came free from calling `ShopPricing.SellPrice` instead of writing the multiply. The appraiser has the same temptation in a sharper form — it *reports* prices, so a second computation would be invisible until a player was quoted a number the game refuses to pay.
    2. ⚠️ **Render the people, not just the props.** Four placements faced the wrong way here and the `.tscn` read fine for all four. Any 38P2 placement gets the same treatment before it is called done.
  - **Reachable when next played:** `travel goto travel.ember_crown.embermarket`, then east past the stalls. Ask Mirelle why you would wait three days. List something with a stone in it, sleep at the inn three times, and take the ticket to Halder at the counter behind her.
- [x] **38P2 — The appraiser** `[F/C]` ✅ *(the second half of 38P, split at the maintainer's call)*
  - **Done when:** the appraiser names the best buyer for what the player is carrying, computed from the same functions the sale uses.
  - **Landed:** `EconomyReport.BestBuyers` + `BestConsignment` extracted public and a broker block added to the arbitrage table, `ServiceKind.Appraise` and its verb, `AppraisalOpenedEvent`, `AppraisalPanel` (1 new C# file), **2 validator rules**, **1 service**, 1 prop-interactable in the Embermarket, 9 locale rows and one rewritten dialogue line. **Plus a collider corrected in a cell this sub-phase does not otherwise touch.** No new art.
  - ⚠️ **38P SHIPPED A DEFECT AND THIS FOUND IT, WHICH IS THE BEST ARGUMENT FOR THE EXTRACTION.** `TryBestRoute` quoted a sell price for *every* shop whose `Accepts` passed — including the consignment house, whose `SellFraction = 0.4` is inert data the vendor window never reads because it branches on `IsConsignment`. So the realm's price report offered Mirelle as an outright buyer at a price the game refuses to pay: exactly the failure `EconomyReport`'s own header was written to warn against. **One `continue` in `BestBuyers` is the fix**, and it is in the extracted function rather than in two places because that is the whole point of extracting it.
  - ⚠️ **BUT THE DEFECT WAS LATENT, NOT ON SCREEN, AND SAYING SO MATTERS.** The before/after `--economy` diff is *only* the new broker block — not one route row changed. She was never anybody's best buyer: her 0.5 on her two specialties loses to the jeweller's and the curioseller's 0.5625. **The fix corrects a computation whose output happened to be invisible today**, and any keener broker or new tag would have surfaced it. Reporting it as "fixed a wrong number in the table" would have been a nicer sentence and false.
  - **The broker block is separate from the table on purpose.** Every row above it is a two-shop *route* with a margin; a consignment is one counter paid days later with no second end to subtract. Forcing it into a route column meant either a fake buy price or a margin column that means something different on one line than on all the others.
  - ⚠️ **`default(Offer)` HUNG THE HEADLESS RUN, AND THE DEFENSIVE CODE IS WHAT MADE IT HANG.** The record struct carried `public string Shop { get; } = Shop ?? string.Empty;` — which does not run for `default`, because that bypasses the primary constructor entirely. So `Shop` was null, `Shop.Length` threw inside `GameBootstrap._Ready`, `quit()` was never reached, and `--economy` sat there for five minutes. **38O's rule paid for itself within one session of being written down**: read the log before blaming the machine, and the NRE was in it the whole time. Fixed at the root with a `Has` property that cannot be null-dereferenced, and the trap is recorded on the type.
  - ⚠️ **`prp_tome_stand` WAS ADOPTED AND REJECTED ON SIGHT, THEN TURNED OUT TO BE MIS-MEASURED TWICE OVER.** It renders as a grey stone plinth — right for the Ancient Aerie it was adopted for, a temple monument parked among timber stalls here. It went in an assay table instead, which is the market's own material and is what Nessa's line describes.
  - ⚠️ **AND THE REAL FINDING IS UNDERNEATH THAT: MEASURING THE ACCESSORS IS NOT MEASURING THE MODEL.** `prp_tome_stand`'s POSITION min/max read 1.743 × 2.315 × 1.743, so that is the collider that was authored — but its single node carries **scale 0.527**, making the real prop 0.92 × 1.22. The first collider was **over twice too big in every axis**, an invisible wall around a waist-high plinth. Same shape as the documented `Icosphere` trap: a plausible number, wrong by a constant factor, and nothing about the file says so.
  - **The same measurement found a live defect in `ancient_aerie.tscn` and it is fixed here rather than filed.** That cell gives the same model a `(0.5, 0.7, 0.4)` box — half a metre of solid-looking plinth with no collider at all, NOW.md invariant 15. One model, two placements, one wrong number: the fix belongs at the model's true size, not in the cell that happened to notice.
  - **The panel is the first in the game that only reads**, which is why it is a third of `StoragePanel`'s length — all of that file is its transfer surface. Every number on it comes from `EconomyReport`, which prices through the same `ShopPricing` calls `VendorPanel` charges, so a valuation the game will not honour is unauthorable rather than merely unlikely. ⚠️ **Prices are quoted PER UNIT even for a stack of twenty**: 38H's saturation drops the payout as a stack crosses the counter, so a multiplied total would be the one figure on screen that is genuinely a lie.
  - ⚠️ **Free, and `--validate` enforces it — 38O's priced-search rule for the third time.** `ServiceRules` refuses an unaffordable service *before* the verb runs, so a fee fails closed on the player with an empty purse and a full pack, who is exactly the person that walked over to ask. A perfectly good gold sink was deliberately not taken, and the reasoning is now in `DESIGN.md` §6 so nobody re-litigates it. **That table also gained 38P's missing commission row** — 38P added a live sink and did not record it.
  - **No new unit tests, and that is honest rather than lazy.** `EconomyReport` is Godot-bound (`ShopDatabase`, `ItemDatabase`, `ServiceLocator`) and cannot be constructed in `Embervale.Tests`; the arithmetic beneath it is already pinned by `ConsignmentRulesTests` and `ShopPricingTests`. `--economy`, captured before and after, is the check — which is the same argument that made `--economy` exist in 38N1.
  - Build clean + **1198** tests + `--validate` exit 0 + `--economy` diffed before/after + `--state` captured + the counter rendered at noon and dusk from four positions across two props + a `--play` boot with all nine cells resident and **0 project errors**. Both new rules negative-tested both ways on one resource, each naming its own field.
  - ⚠️ **What was NOT verified, and it is more than usual.** The `F1` console needs a human and the MCP cannot inject input, so **the interact press, the entire `AppraisalPanel` — every row, every price on screen, the sort order and the empty state — and Nessa's rewritten line are *reviewed against the Godot 4.7 C# API*, not observed.** The panel is the largest unobservable thing either half of 38P shipped; a clean `--play` boot proves the game starts, not that the window draws.
  - Two things worth carrying into 38Q:
    1. **A number measured off a file still needs its transforms applied.** Node scale, `nodes/root_scale`, and a parent's scale all sit between the accessors and the world. Two colliders were wrong here for exactly that reason, in two different cells, by two different factors.
    2. ⚠️ **A defensive initializer on a record struct is a lie the compiler tells you.** `default(T)` skips it. If a struct has an "empty" state, give it a predicate that reads the field safely — do not write a constructor that launders nulls and then trust the field.
- [x] **38Q — Commissions** `[F/C]` ✅ *(38Q split in two at the session's outset, the way 38M/38M2, 38N1/38N2 and 38P/38P2 were: the master this session, the contract board as 38Q2)*
  - **Done when:** a master crafts a known recipe for a fee.
  - **Landed:** `CommissionRules` (1 new C# file, pure, 5 tests), `EconomyReport.CommissionCost`, `CraftingComponent.CanMake` + `Commission`, two fields on `CraftingStationOpenedEvent`, `ServiceKind.Commission` + two exports, a fee mode in `CraftingPanel`, **5 validator rules**, **1 service**, 1 prop placement in `town_hub`, 4 locale rows and one dialogue node. **No new panel and no new art** — the model is one the Embermarket already uses.
  - ⚠️ **THE FIRST THING THE BRIEF ASKED FOR WOULD HAVE BEEN INVISIBLE, AND FINDING THAT OUT WAS THE DESIGN WORK.** `town_hub` carries a **free** `StationForge`, `StationWorkbench` and `StationAlchemy` in the open square. A master who charges a fee to craft a recipe the player can already supply is *strictly worse than walking twenty metres* — correct, validated, saved and completely imperceptible, which is 38G's parking notice word for word. The feature is therefore not the labour: **he supplies the ingredients the player did not bring**, at his own counter price. That was put to the maintainer before a line was written, because it is the whole shape of the sub-phase.
  - ⚠️ **AND IT IS THE FIRST PRICE IN THE ECONOMY THE `ShopPricing` CLAMPS DO NOT PROTECT.** Every earlier one was a spread over a *single* item's value, so `sell <= value <= buy` held by construction (38F) and 38P's consignment inherited it for free by calling `SellPrice`. A commission spans **two different items** — ingredients in, a finished piece out — and crafting is *meant* to add value, so nothing in the arithmetic stops buy-materials → commission → sell from paying for itself. It is closed by the labour fee, which is authored data, which is why `CommissionRules.Exploitable` exists and `--validate` runs it over every recipe the counter can reach. **The arc's habit of inheriting invariants ran out here, and the replacement had to be a rule.**
  - **The fee was set by the validator rather than by taste, and the first guess was wrong.** Dropped to a labour fee of 1, exactly one recipe fires: `recipe.kit.alchemy`, 69 gold of materials against the smith's 81 for the kit — so the floor is 12. The header comment originally reasoned its way to "needs a fee over 42" from the same numbers by hand and was simply wrong; the negative test is what produced the real figure. **60 is authored well clear of the floor and not on it**, because the margin an author leaves is the margin a later recipe removes.
  - ⚠️ **CHARGED AFTER THE VERB — THE ONLY SERVICE THAT IS, AND IT LOOKS LIKE A BUG BESIDE THE OTHER EIGHT.** 38D charges before every verb because none of them can fail once paid: a bed, a flag, a taught recipe. A commission fails whenever the pack has no room for the piece, and `Craft` already rolls itself back cleanly when it does — so charging first would be the one path in the whole battery that loses the money for nothing. The exemption is one named local in `ServiceComponent.Interact` with the reason on it, and the money lives in `CraftingComponent.Commission` beside the rollback it depends on.
  - **The window is `CraftingPanel` with a fee on it, and adding no second panel is most of why this fitted in a session.** `CraftingStationOpenedEvent` gained `LabourGold` and `MaterialsShopId`, both **defaulted to the ungated case**, so `CraftingStationComponent` — every free station in the game — publishes exactly what it always did and needed no edit. 38I's trick, third outing. The one real change inside the panel is that the Craft gate becomes *affordability* rather than *materials*, and a shortfall chip turns from red to accent: same numbers, opposite meaning, and leaving it red would have told the player the working button was broken.
  - **`CanCraft` was split into `CanMake` + `HasIngredients`** rather than the panel asking the two questions itself. A master needs everything a craft needs *except* the materials, because the materials are what he is selling — so the two questions had to stop being one, in the component that owns them.
  - ⚠️ **`prp_weapon_stand` WAS ADOPTED, PLACED, AND REJECTED ON THE RENDER — FIFTH TIME.** A smith's empty weapon rack is the right idea and the wrong object: from every angle but straight-on the slots are invisible and it reads as a **sawhorse**, so the prompt would have invited the player to commission a sword from a trestle. Nothing in the `.tscn`, the filename or the measurements said so. `prp_market_stand_b` was rendered beside it and rejected too — a canopied market stall in the town square is the wrong district and dominates the corner. `prp_table_large` won, and its collider is the number `embermarket.tscn` already records for the assay counter rather than a fresh measurement.
  - ⚠️ **AND THE FIRST TWO PLACEMENTS WERE BOTH WRONG IN WAYS ONLY THE RENDER SHOWED.** `(-8.6, -3.2)` put the bench inside `BuildingNW`'s corner; `(-5.8, -4.5)` put **Bryn standing inside his own counter**, because a 2.85 m table's footprint reaches the NPC four metres away and nothing computes that. Third position holds. 38P's "render the people, not just the props" is now "render the people *and* the furniture, together" — the fault was in neither object alone.
  - **`--economy` is byte-identical before and after, and that is the expected result rather than a disappointment.** A commission is not a route: it has no second shop to subtract, so the arbitrage table cannot move. The diff was taken against a fully stashed tree (including the untracked service) so the claim means what it says.
  - **Five rules negative-tested both ways**, each firing its own message and each restored from a backup copy rather than through git. Two notes on the pairs that fired together: breaking the station to `Hand` **also** trips the empty-window rule, because `StationAccepts(Forge, Hand)` is false for every recipe in the game — 38L's masking trap in miniature, and the second message is not evidence about the first. Setting the fee to 0 **also** trips the anti-arbitrage rule, which is not masking at all: it is the two rules together spelling out exactly why free is forbidden here.
  - **`--validate` enforces that this service is PRICED, which inverts a rule that had fired three times running.** 38O's search, 38P's collect counter and 38P2's appraiser are all forced *free* because `ServiceRules` refuses an unaffordable service before the verb runs, so a fee fails closed on the player who most needs the counter. A commission hands over *goods*, so the same reasoning inverts. Both the rule and the `.tres` header say so at length, because a fourth free service is exactly what a reader would "correct" this to.
  - Build clean, **0 warnings** + **1203** tests (5 new) + `--validate` exit 0 with all five new rules broken and restored + `--economy` diffed before/after + `--state` captured + the counter rendered at noon and dusk from four positions across **three placements and three candidate props** + a `--play` boot with all nine cells resident, 27 objects restored and **0 project errors** (two WASAPI audio lines, no sound device in this environment).
  - ⚠️ **What was NOT verified, and it is the transaction.** The `F1` console needs a human and the MCP cannot inject input, so **the interact press, the priced Commission button, the quoted number on screen, the charge, the materials being handed over, and all three rollback paths (too poor, pack too full for the materials, pack too full for the piece) are *reviewed against the Godot 4.7 C# API*, not observed.** A clean `--play` boot proves the game starts, not that a single order was placed. The arithmetic beneath it is the part that has tests.
  - Two things worth carrying into 38Q2:
    1. **Ask what the new thing is worth next to what already exists, before building it.** The free forge twenty metres away nearly made this whole sub-phase decorative, and nothing in the brief, the roadmap or the playbook mentioned it — it took reading `town_hub.tscn`. 38Q2's board has the same exposure in a sharper form: the town already has a guild board and the market a notice board, and a third board the player must learn to tell apart is worse than no board.
    2. ⚠️ **An invariant you inherit is not an invariant you have.** Six sub-phases of "it routes through `ShopPricing`, so the spread survives" made that reasoning feel automatic, and it is false the moment a price relates *two* items instead of one. When a new price appears, ask what it is a spread *over* before assuming the clamps cover it.
  - **Reachable when next played:** walk west from the waystone to the smithy corner. Ask Bryn "would you make something for me?", then press `E` on the long table beside him: every forge recipe you know is listed with what it will cost, and the ones you have no metal for are priced rather than greyed. Bring three iron ingots and a leather strip and the steel sword costs the bare 60; bring none and his metal is added to the bill at the same price his shelf charges.
- [x] **38Q2 — The contract board** `[F/C]` ✅ *(the second half of 38Q)*
  - **Done when:** a board offers rotating supply contracts — deliver N of an item by a day for gold and standing — that never pollute the quest log.
  - **Landed:** `ContractRules` (pure, 7 tests), `ContractResource` + `ContractDatabase` + a `data/contracts/` folder, `ContractLedger` (`ISaveable`), `ServiceKind.Contracts` + two exports, `ContractBoardOpenedEvent`, `ContractBoardPanel` (5 new C# files), **7 validator rules**, **8 contracts**, **1 service**, a board in the Crossway yard, 18 locale rows and a `--state` census line. **No new art at all** and **nothing touching the quest system**.
  - ⚠️ **THE ROTATION IS DERIVED FROM THE DAY AND NEVER STORED, AND THAT IS THE WHOLE SUB-PHASE.** `ContractRules.Cycle(day, 4)` plus a fixed integer scramble is the entire board, so the same day always shows the same three postings and **a quickload cannot reroll it** — which is 38S's stated rule ("bound to the day in saved state so a reload cannot reroll it") arriving a sub-phase early and for nothing. The alternative — roll a rotation, save it — needs a second thing that can disagree with the clock. `ContractLedger` therefore records **only what the player filled**, never what was offered, and that asymmetry is the design rather than an economy.
  - **Distinct by construction, not by retrying.** The three slots come from `start + slot * stride` with the stride forced coprime to the pool, so two slots can never land on the same posting for *any* pool size. A reject-and-resample version would have been shorter and would have failed silently on a pool barely larger than the board — which is exactly the case a small realm has, and the case a spot-check passes. The test sweeps every pool size 2–24 across 65 cycles.
  - ⚠️ **THE REWARD RULE RUNS THE OPPOSITE WAY TO 38Q's, AND THE PAIR IS WORTH READING TOGETHER.** A commission is refused for being too **cheap**, because materials-in and goods-out could be looped forever. A contract is refused for being too **poor**: it is one-shot per rotation, so nothing can be looped, and a posting paying less than a merchant already pays is a longer walk for less money — 38G's imperceptibility failure in a new hat. **The bound is `ContractLedger`, never the price**, which is why there is deliberately no ceiling: buying cheap and delivering dear is a real trade and is meant to be.
  - ⚠️ **`ItemType.Quest` HAS NO MEMBERS IN THE WHOLE GAME, AND THE RULE AGAINST IT COULD NOT BE TESTED WITHOUT AUTHORING ONE.** `ShopPricing.Sellable` has refused quest items at every counter since 38A and has never had anything to refuse; the new contract rule is the second reader of a type with zero instances. Negative-testing it meant temporarily flipping `item.material.coal` to `Type = 5` — at which point **two** rules fired, this one and the pre-existing "shop stocks a quest item" one, both correct. This is `TradeTags.cs`'s "a tag with no members is a promise rather than a feature" one layer down, and it is worth someone deciding whether Phase 39+ ever authors one.
  - ⚠️ **THE BANNER WAS PLACED AND REJECTED ON THE RENDER, FOR TWO REASONS AT ONCE — sixth time this trap has fired.** `prp_banner_guild` is what the Embermarket's notice board and town_hub's guild marker both use, so it looked like the settled answer. It is a hanging cloth **pennant**: at reading distance it fills the view as a red sheet with no surface to post anything on. And **this cell already spends that model on `GateBanner`**, whose own comment calls it "the only thing in the cell that says whose road this is" — two identical pennants forty feet apart would have diluted the one that means something. Neither fault is visible in the `.tscn`, and the second is only visible with both objects in frame.
  - **The replacement is three boxes, and reaching for primitives was the right rung rather than a defeat.** The cell's floor, road and yard are already `BoxMesh` + `StandardMaterial3D`; a plank face on two legs is the same vocabulary, reads as a board from every angle including behind, and needed no model, no Blender pass and no library search. ⚠️ The asset rule is *the four packs first* — but the packs have no notice board, and a wrong-shaped model adopted for the sake of the rule is worse than a right-shaped box.
  - **No dialogue line points at it, and that is a finding rather than a cut.** The plan called for one, and there is nowhere to put it: **all four Crossway NPCs are `ServiceComponent`s** (permit, bribe, search, impound) and the cell has **no conversation at all**. A board is also the one interactable in the game that advertises itself — it is a sign. Recorded because "add a line to the wardens' conversation" reads as an obvious step until you look.
  - **The ledger's save round trip WAS exercised, which 38P's was not.** `ContractLedger` is a Godot `Node`, so `Embervale.Tests` cannot construct it — but it is `[GlobalClass]`, so a throwaway `SceneTree` GDScript instantiated it directly and checked seven properties headlessly, including **the §7 replace-never-merge rule** (loading an empty save must *un-fill* a posting, or a delivery made in an abandoned timeline survives into the restored one). All seven passed; the harness was deleted. **That technique is the finding**: a `[GlobalClass]` C# node is reachable from GDScript by name, which is a way to exercise Godot-bound logic the xUnit project can never touch.
  - **Seven rules negative-tested both ways**, each firing its own message, every file restored from a backup copy. Two pairs fired together and both are attributable: breaking quantity and reward at once **also** trips the reward-floor rule (a zero reward is below any shelf — the same fault seen twice, not masking), and the quest-item break trips the shop rule as described above. ⚠️ The first attempt at the quest-item test pointed the contract at an item id that **did not exist**, so it tripped the unknown-item rule and proved nothing — 38L's masking trap, caught by reading the message rather than the exit code.
  - Build clean, **0 warnings** + **1210** tests (7 new) + `--validate` exit 0 with all seven new rules broken and restored + `--state` showing the new census line + `--economy` **byte-identical** before and after (a contract is not a route) + a `--play` boot with all nine cells resident, 27 objects restored and **0 project errors** + the board rendered noon and dusk from six positions across **two candidate props**.
  - **A pre-38Q2 save warns and carries on**, as 38O's and 38P's did: `--play` on an old slot logs *"no usable entry for 'contracts'"* and the ledger keeps its empty state. Correct, and worth knowing before it is mistaken for a defect.
  - ⚠️ **What was NOT verified.** The `F1` console needs a human and the MCP cannot inject input, so **the interact press, every row of the board, the Deliver button and its disabled state, the goods leaving the pack, the gold and standing arriving, and the greyed filled row are *reviewed against the Godot 4.7 C# API*, not observed.** The rotation arithmetic and the ledger's persistence are the parts that were actually run.
  - Two things worth carrying into 38R:
    1. **Derive it from the clock before you decide to save it.** The whole of this board's persistence is one dictionary of what the player did, because everything else was a function of the day. 38R's seven services and 38S's haggling both have state that *looks* like it must be stored — ask what it is a function of first.
    2. ⚠️ **A model that two other cells already use is not thereby the right model.** The banner was chosen because the two existing boards use it, which is precedent rather than judgement, and it was wrong on its own merits *and* wrong for this cell specifically. The second kind of wrong is only visible with both objects in one frame.
  - **Reachable when next played:** `travel goto travel.ember_crown.crossway_post`, then east into the caravan yard between the carts and the fire. `E` on the board lists three postings, what you are carrying against each, and what they pay; the footer counts down to the day they change. Sleep at the inn four times and the board is different.
- [x] **38R — Services II** `[F/C]` ✅ *(38R split in two at the session's outset, the way 38M/38M2, 38N1/38N2, 38P/38P2 and 38Q/38Q2 were: the conversational route and the hire this session, courier and gambling as 38R2. Four of the seven briefed services never reached the split — see below)*
  - **Done when:** ~~courier, passage, mercenary, cosmetic, healer, warehouse and gambling are interactable~~, and the innkeeper's conversation is restored through `OpenService`. **Amended at the maintainer's call:** four of the seven were struck rather than built, one is built, two moved to 38R2.
  - **Landed:** `DialogueEffect.OpenService` + `ServiceComponent.TryUse` (the pay-and-grant path extracted, no behaviour change), `ServiceKind.Mercenary` + `ServiceResource.CompanionId`, **6 validator rules**, a `--service` mode on `tools/gen_merchant_dialogue.py`, **2 conversations**, **1 service**, **1 companion**, one NPC in the Crossway yard, 24 locale rows, and 6 ordinal assertions in `EnumStabilityTests`. **No new C# file, no new panel, no new art, no new model.**
  - ⚠️ **FOUR OF THE SEVEN BRIEFED SERVICES WERE STRUCK, AND FINDING THAT OUT WAS MOST OF THE DESIGN WORK.** 38Q's rule — ask what the new thing is worth next to what already exists, *before* building it — applied four times in one reading:
    - **cosmetic:** there is **no player appearance system anywhere in the game**. `CharacterProfile` is fixed at creation and `CorruptionAppearanceController` is the only thing that ever changes a look. A cosmetic service would have had nothing to change: the stub 40B forbids, not a feature.
    - **healer:** `ServiceKind.Inn` already refills **every** resource stat and moves the clock for 10 gold. A healer that heals is strictly worse than the bed — 38Q's free-forge failure exactly. The only persistent condition in the game is corruption, and `DESIGN.md` §5 forbids money buying it away, so the one version that *would* be perceptible is a design change rather than a service.
    - **passage:** already shipped, as 38M. `ServiceKind.Passage` sells the Crossway permit and the gate-hand's bribe, and fast travel charges `TravelFee`. The line was stale, and building to it would have produced a second toll.
    - ⚠️ **warehouse:** struck **mid-session, after the `.tres` was already being written**, because `EmberCrownBank.tres`'s own header had ruled on it in 38D: *"Deliberately one vault rather than one per settlement: a bank you cannot reach from the next town is a chest, and the cottage already has a chest."* A warehouse is `ServiceKind.Bank` a second time, opening the same `StoragePanel`; "storage staged beside the contract board" does not survive that, because nothing about a contract or the toll reads the vault. **The plan cleared review with the warehouse in it — the ruling was in a resource header, not in the roadmap, the playbook or `DESIGN.md`.** Read the `.tres` you are about to copy, not just the one you are about to write.
  - ⚠️ **`DialogueEffect.OpenService` IS THE SUB-PHASE, AND 38E NAMED IT THREE SUB-PHASES AGO.** 38E appended `OpenShop` and deliberately did *not* append its twin, in one of the clearest deferrals in the playbook: "firing a service without a `ServiceComponent` means refactoring its pay-and-grant path, and an enum member with no implementation is the stub 40B forbids. It lands in 38R with the innkeeper's conversation." That refactor is four lines of signature: `Interact` became a call to a static `TryUse`, and every verb was already static except the vault.
  - ⚠️ **AND THE VAULT IS WHY IT IS NOT A PURE EXTRACTION.** Every service verb works off the player and the resource — except `Bank`, which opens the **host entity's** inventory, and a conversation has no host entity. `TryUse` therefore takes a nullable vault, and the new rule that is not a copy of an existing one refuses `OpenService` on a `Bank` **in the data**. A runtime warning would have been a choice that silently does nothing, which is 38J's dead-choice failure with a log line attached.
  - **The innkeeper's `ServiceComponent` is GONE, not beside the conversation.** 38D deleted his dialogue to make room for the service, on the rule that an entity gets one interactable; 38R deletes the service to put the conversation back. Both directions of that trade are now written into `town_hub.tscn` at the same node. ⚠️ **The bed is now reachable only through `dialogue.innkeeper`** — a broken id there is not a missing conversation, it is the realm's only inn closing.
  - ⚠️ **A MERCENARY IS THE SECOND SERVICE CHARGED AFTER ITS VERB, AND IT NEEDS NO ROLLBACK, WHICH IS THE INTERESTING HALF.** `CompanionRoster.Recruit` returns false on a full party, so gold taken at the counter buys nobody — 38Q's inversion, second instance. But the commission's rollback machinery is *not* needed here: a commission is paid from a window the player may leave open for minutes, while a hire resolves inside the same synchronous call as the affordability check, so the purse cannot move between the two. **Same ordering, half the code, and the reason is the window and not the verb.**
  - **A full party is a prompt state rather than an outcome, and folding it into "already held" would have lied.** `ServiceOutcome` gained nothing: one `when` clause in `Prompt` says *you have no room in your band for her*. Answering it with the already-held line would have told a player who has never met Wren that she already walks with them.
  - **The hire is recorded by the roster and by nothing else, and `--validate` refuses a flag beside it.** Every other one-off purchase in the battery writes an `UnlockFlagId`; this one must not, because a flag survives a **dismissal** — dismiss her once and she could never be hired again, a receipt outliving the thing it was a receipt for. The already-held test asks `CompanionRoster.IsRecruited`.
  - **The generator grew a `--service` mode rather than two hand-written files.** The service shape differs from the merchant one in three ways that are each a decision: `Effect = 10` and no `Goto`; **no hours and no closed node** (services keep none — `DESIGN.md` §6, an inn that shut at night would be the only way to pass the night, shut at night); and no price in the graph, because a walk-up prompt derives the number and a conversation says it in authored prose. Two files was the break-even; the third and fourth are 38R2's.
  - ⚠️ **THE FIRST PLACEMENT PUT HER WHERE THE PLAYER STANDS TO READ LAST SUB-PHASE'S BOARD — SEVENTH FIRING.** `(10.6, 0, 6.4)` is three metres due south of the caravan board, which is its reading position; the front-on render came back with the board filling the frame behind her. **The fault was in neither object and neither `.tscn` line.** ⚠️ And the body faces **+Z**, not Godot's −Z, so the identity transform pointed her at the board's back — a fact no file states and only two opposed eye-level shots settle. Final: `(12.6, 0, 8.9)`, yawed west across the yard to the road, beside the board rather than in front of it.
  - **The body needed no search: `npc_adventurer_f` was already adopted and already retargeted.** It is the same model Sera wears in the Embermarket, which 38Q2's duplicate rule permits — that rule is about two identical models **in one frame**, and these are cells apart. What *is* in frame are the two guild-coat wardens up the road, which is the argument for a fighter's kit rather than against it.
  - **This cell had four services and not one conversation**, which 38Q2 recorded as a finding when it went looking for somewhere to mention the board. A permit, a bribe, a search and a fine are transactions; a sword for hire is not, and she is the Crossway's first speaking part.
  - **No new unit tests, and that is honest rather than lazy** (38P2's argument, second outing). Everything 38R added is Godot-bound — `TryUse`, `Hire`, `PartyIsFull` and the six rules all need `ServiceLocator`, a roster or a database. What *was* added is six ordinal assertions, and they found real rot: ⚠️ **`ServiceKind_Ordinals` had drifted five members behind the enum** because 38P, 38Q and 38Q2 each appended a member and none extended the test — and `CrosswayMercenary.tres` says `Kind = 11` as a bare integer.
  - Build clean, **0 warnings** + **1226** tests + `--validate` exit 0 with **all six new rules broken and restored, each firing its own message alone** + `--economy` **byte-identical** before and after (a hire is not a route: no second shop to subtract) + `--state` showing 13 services, 33 dialogues, 2 companions + a `--play` boot with all nine cells resident, 27 objects restored, Kael restored and **0 project errors** + the mercenary rendered noon and dusk from five positions across **two placements and two facings**, with the board and the wardens in frame.
  - ⚠️ **What was NOT verified.** The `F1` console and the interact press need a human, so **the innkeeper's new choice and the bed behind it, Wren's hire, the 500 gold leaving the purse, the full-party refusal, and every line of both conversations are *reviewed against the Godot 4.7 C# API*, not observed.** The route itself is the biggest unobserved thing here: `DialogueEffect.OpenService` has two authored callers and neither has been pressed. The refactor beneath it is behaviour-preserving by construction — `Interact` calls `TryUse` with the vault it used to read itself — and `--play` proves the cells build and the databases load, not that a room was taken.
  - Two things worth carrying into 38R2:
    1. ⚠️ **A ruling can live in a resource header, and the header is not a doc anybody greps.** The warehouse survived the brief, the plan and a review, and was killed by a comment in the `.tres` it was about to be copied from. Before authoring content of a kind that already exists, **read the existing one's header** — `docs/` is not where every decision was written down.
    2. **Ask whether the thing already exists under another name before asking what it is worth.** Four of seven struck, and *two* of those (passage, warehouse) were not weak features — they were features the game had already shipped. The 38Q question ("what is it worth next to what exists?") has a cheaper question in front of it: "is this the same thing?"
  - **Reachable when next played:** talk to Innkeeper Holt in the town hub — the bed is a line in the conversation now, not a press on the man. Then `travel goto travel.ember_crown.crossway_post` and walk east into the caravan yard: Wren Halloway stands by the sacks facing the road, and five hundred gold puts her behind you. Dismiss her and she is standing there again.
- [x] **38R2 — The dockside bones** `[F/C]` ✅ *(the second half of 38R; retitled from "Courier + gambling" when the courier was struck)*
  - **Done when:** ~~a courier moves goods or coin between settlements for a fee, and~~ a game of chance takes and pays out on a rotation a quickload cannot reroll.
  - **Landed:** `WagerRules` (pure, **11 tests**), `WagerLedger` (`ISaveable`, exercised headlessly), `ServiceKind.Wager` + three exports, `WagerSettledEvent` + one `Notifications` handler, **6 validator rules**, **1 service**, a table and two barrels at Hollowreach, 5 locale rows, 1 ordinal assertion. **No new panel, no new art, no new model** — two new C# files, both small.
  - ⚠️ **THE COURIER WAS STRUCK, AND IT IS THE FIFTH OF 38R's SEVEN TO GO.** Fast travel is **15 gold** within a region and **free to a holding you own** (`TravelFee`), and it moves the player *and* everything they carry — so a courier that moves only the goods has to undercut a 30-gold round trip to be worth anything, at which point it saves the player five gold and a loading screen. The pressure it would relieve is not weight either: ⚠️ **`MaxWeight` and `IsOverEncumbered` are computed by `InventoryComponent` and read by NOTHING in `src/`**, so the only real constraint is slot count. **Second of the seven struck because the game had already shipped it under another name**, after passage — which is 38R's own carried lesson applied to the sub-phase it was written for.
  - ⚠️ **AND THE PLAYBOOK LINE ABOVE THIS ONE WAS WRONG, SO IT IS CORRECTED RATHER THAN QUIETLY DROPPED.** 38R called gambling "a gold SOURCE, the second in the arc". Per *event* it is; in *expectation* it must be a **sink**, or the throws-a-day cap is the only thing between the player and an infinite purse. The correction is the whole reason the expected-value rule exists.
  - ⚠️ **THE RULE THAT MATTERS IS THE EXPECTED VALUE, AND IT HAS TO BE CHECKED AT THE PLAYER'S BEST STANDING — 38Q's TRAP IN A PLACE WITH NO CLAMPS BEHIND IT.** `PriceOf` runs the stake through `ShopPricing.ServicePrice`, so an Allied player stakes **15% less against a payout that does not move**. A table authored at 50/25%/**175** expects to *lose* the player money at Neutral (4375 against 5000) and to *make* it at Allied (4375 against 4300) — that exact case was authored, run and refused, and no arithmetic anywhere else in the economy would have caught it. A wager is not a spread over anything, so the 38A clamps have nothing to say about it.
  - **The numbers are authored clear of the printed floor, not on it** (38Q's habit, second outing). 50 gold a throw, one in four, 150 back: −12.5 expected a throw, about −37 a day played out. The ceiling at Allied is a payout of 172; 150 sits well under it, because the margin an author leaves is the margin a later edit removes.
  - ⚠️ **`FactionId` IS AUTHORED PRECISELY TO MAKE THAT RULE LIVE.** A house with no faction never meets the standing ramp, so its Allied case is unreachable and the rule would be guarding nothing. Pointing the table at `faction.outlaws` makes the discount real — and is the first thing in the economy that outlaw standing buys.
  - **The outcome is derived and the ledger is the bound, and those are two different jobs.** `WagerRules.Won(day, playIndex, houseId, chance)` is a pure function, so a quickload **replays** a loss rather than rerolling it — 38Q2's rule, needed more here because a saved roll is a roll a reload can re-take. But nothing in that stops a hundred throws in one afternoon: `PlaysPerDay` does, through `WagerLedger`, and a negative expectation is only a certainty over a bounded number of tries.
  - ⚠️ **`string.GetHashCode()` WOULD HAVE BROKEN ALL OF IT, SILENTLY.** .NET randomises string hashing per process, so folding the house id in that way would make the same day pay differently after a restart — which is the one property the design exists to provide, and it would have looked exactly like an RNG working. `HouseSeed` is a hand-written FNV-1a, and the test that catches a regression is the one pinning a **hard-coded** win/loss string: a self-consistent test would have passed either way.
  - **Charged BEFORE the verb, and saying so is worth a line** because the last two sub-phases both added exceptions. A throw cannot fail: the stake goes, the outcome is read, and a payout is gold, which stacks into any pack that had room for the stake. The `chargedAtTheCounter` list stays two long and did not become a field.
  - **A wager opens no window, so the toast IS the result.** One event and one handler in `Notifications` — without it a loss is a gold counter falling for no stated reason, which is the state a player reports as a bug. That is also the whole of the UI work, and it is why this fitted beside the rules and the ledger in one session.
  - ⚠️ **38Q2's RECORDED TECHNIQUE FOR EXERCISING A GODOT NODE HEADLESSLY IS WRONG AS WRITTEN, AND THAT IS THIS SESSION'S MOST REUSABLE FINDING.** It says a `[GlobalClass]` C# node "is reachable from GDScript **by name**". It is not: both `ClassDB.instantiate("WagerLedger")` and a bare `WagerLedger.new()` fail in a headless `--script` run, because the global class cache is only refreshed by an **editor import** and a new `.cs` file has no `.uid` yet. What works with no import at all is **`load("res://src/Economy/WagerLedger.cs").new()`**. Ten checks then ran, including the §7 replace-never-merge rule (an empty save must give a spent throw *back*) and a malformed row.
  - ⚠️ **THE TABLE IS THE THIRD USE OF `prp_table_large` AND ITS SECOND AS AN INTERACTABLE**, after Bryn's order bench and Nessa's assay counter. 38Q2's duplicate rule permits it — that rule is about two identical models **in one frame**, and these are cells apart — but a player who has learned "long table = counter" walks up to this one expecting one. **The fix was two barrels, one either side**: the only table in the realm with seats facing each other, built from a prop the cell already carries. A fifth crate was the cheaper idea and was refused before it was drawn, because the cell has four already and "which crate is the game?" is a problem that needs no render to see.
  - **Six rules negative-tested both ways**, each restored from a backup copy. Two fired in pairs and both are attributable rather than masking: a **zero stake** and a **100% chance** are each independently a positive expectation, so the EV rule fires alongside its own rule and is saying the same thing twice on purpose.
  - Build clean, **0 warnings** + **1237** tests (11 new, and the first genuinely testable logic since 38Q2) + `--validate` exit 0 with all six new rules broken and restored + `--economy` **byte-identical** before and after (a wager is not a route) + `--state` showing 14 services + the ledger's save round trip **actually run** headlessly + a `--play` boot with all nine cells resident, 27 objects restored and **0 project errors**, logging the expected *"no usable entry for 'wagers'"* on the pre-38R2 save + the table rendered noon and dusk from four positions, before and after the barrels.
  - ⚠️ **What was NOT verified.** The interact press needs a human, so **the throw itself, the stake leaving the purse, the payout arriving, both toasts, the throws-left prompt and the spent-for-today line are *reviewed against the Godot 4.7 C# API*, not observed.** The arithmetic and the ledger are the parts that were actually run, and between them they cover every guarantee the design rests on — which is more than the last three sub-phases could say.
  - Two things worth carrying into 38S:
    1. ⚠️ **A price the player can WIN is not a price the standing ramp is safe on.** Every rule in this arc has been checked at the cheapest standing since 38Q, and each time it was because a discount moved one side of a relation and not the other. 38S moves prices *directly*: a haggle that stacks with the ramp is the same shape of fault, and the test for it is to evaluate at Allied rather than at Neutral.
    2. **Derive, then bound — and notice they are two separate mechanisms.** The day makes a result unrerollable; only a ledger makes it unrepeatable. 38S's once-per-merchant-per-day negotiation needs both, and 38Q2's board only needed the first because a posting is filled once by nature.
  - **Reachable when next played:** `travel goto travel.ember_crown.hollowreach`, then east past the cart to the table between the tent and the carts, a barrel either side. `E` stakes fifty gold; the prompt counts your throws down and the toast says which way the bones fell. Come back tomorrow for three more.
- [x] **38S — Haggling + merchant memory** `[F]` ✅
  - **Done when:** ~~a once-per-merchant-per-day negotiation with a standing downside sets that day's prices, bound to the day in saved state so a reload cannot reroll it.~~ Done as written, in one session and with no split.
  - **Landed:** `HaggleRules` (pure, **10 tests**), `HaggleLedger` (`ISaveable`, exercised headlessly), `StableRoll` (lifted out of `WagerRules`), two `ShopResource` exports, a haggle row in `VendorPanel`, **6 validator rules** (4 new + 2 existing ones re-asked with the deal struck), 3 merchants authored, 5 locale rows. **No new panel, no new art, no new model, no new event** — three C# files, all small, and the sweep test extended.
  - ⚠️ **THE ANSWER TO INVARIANT 3's QUESTION CAME BACK "COVERED", AND THAT IS THE FINDING.** Every sub-phase since 38Q has had to ask what a new price is a spread *over*. A haggle is a spread over **one item's value**, exactly like standing and the specialty premium, so `BuyPrice`'s `>= 1` and `SellPrice`'s `0..1` clamps hold it with no new argument — which is why this shipped in one session while a commission needed a bespoke `Exploitable`. **The question is cheap and the answer is sometimes "nothing to do".** What it did need was the *other* half: the clamps stop a money printer, they do not stop a round trip costing nothing.
  - ⚠️ **THE REAL RULE IS THE ROUND-TRIP MARGIN, AND IT IS AN EXISTING RULE ASKED A HARDER QUESTION.** 38F's `ValidateShopTrade` held the widest sell against the narrowest buy with a 1.25 margin; folding the haggle into both sides **tightens the permitted fraction/markup ratio from about 0.52 to about 0.42** for a haggling shop. Negative-tested in the most useful way available: `SellFraction 0.70` on Aldreth is refused **with haggling on and passes with it off**, which proves the tightening is the haggle's doing rather than a coincidence of the numbers. No authored shop was near the new edge (the worst is Marta at 0.36).
  - ⚠️ **THIS IS THE FIRST THING IN THE GAME TO MOVE THE SELL SIDE OF THE SPREAD, AND 38C's ARGUMENT AGAINST DOING SO STILL STANDS — IT JUST DOES NOT APPLY.** `MarkupFor`'s comment refuses standing on the sell side because a generous fraction converges on `sell == buy`. A haggle may, because it is **bounded to one merchant for one day by the ledger**, where standing is permanent. The bonus is deliberately smaller than the discount (1.10 against 0.90) for the same convergence reason. A negotiation the player could only use while buying would have been half a feature to anyone selling loot, and Marta — the realm's best payer, hardest to talk round — is the merchant that half was for.
  - **Derive and bound, both, and the two do different jobs** (38R2's carry, applied). `HaggleRules.Succeeds(day, shopId, chance)` is pure, so a quickload replays a refusal; `HaggleLedger` stores **only that the player asked**, never the answer, so nothing can disagree with the clock (38Q2's rule, third outing). ⚠️ **The attempt is what is saved and the outcome is what is derived** — storing the outcome would have been the obvious shape and is the one that rots.
  - **`WagerRules`' hash came out into `StableRoll` rather than being copied**, and the move was safe to make *because* 38R2 pinned a hard-coded win/loss string: a self-consistent test would have let a refactor change every result silently. The new pin is `HaggleRulesTests.Succeeds_IsStableAcrossProcesses`. Same reason as ever — `string.GetHashCode()` is randomised per process.
  - **The downside is standing rather than a price surcharge**, decided before anything was written. A surcharge is invisible against a spread no player memorised; standing is on the panel, carried to every counter of the faction, and slow to earn back. It also needed no new machinery — `ReputationComponent.Add` and its toast, the `FenceStanding` precedent one screen up in the same file.
  - ⚠️ **A BROKER CANNOT BE HAGGLED, AND THE RULE HAD TO BE WRITTEN BECAUSE THE FAILURE IS SILENT.** `VendorPanel` prices a consignment row through `ConsignmentRules`, which the deal never reaches — so a haggling broker would discount her shelf and not her payout, and read in game as the feature half-working. Invariant 8's fourth absence, found by following the sell-side call site rather than by testing.
  - **Three merchants, one lesson each, and the chances are not a difficulty knob** — the ledger bounds the attempt, so a high chance buys comprehension rather than income. Aldreth 50/−1 in the square (meet it where the answer is often yes), Gilda 40/−2 one street south (the same mechanic, dearer, a minute's walk apart so the difference is legible), Marta 30/−3 up the mountain (the worst odds in the realm on the best payer, on a company store competing with nobody).
  - **`--economy` is unchanged and that is the expected result, not a lucky one.** A haggle moves a multiplier for one day at one counter; the report prints the standing landscape. Every margin is still negative, and **38G is still the only thing that can change that.**
  - Build clean, **0 warnings** + **1247** tests (10 new, plus the arc's sweep test extended with the new multiplier as its own comment demands) + `--validate` exit 0 with all six rules broken and restored + `--economy` identical but for the locale count + `--state` unchanged at 23 shops + the ledger's save round trip **actually run** headlessly, 12 checks including the §7 empty-save case + a `--play` boot with all nine cells resident, 27 objects restored, **0 project errors** and the expected *"no usable entry for 'haggles'"* on the pre-38S save.
  - ⚠️ **What was NOT verified, and it is more than last time.** The button press needs a human, so **the haggle row, the two outcome lines, the standing hit landing and the repriced rows are reviewed against the Godot 4.7 C# API, not observed.** ⚠️ **Nothing was placed in the world, so nothing was rendered** — the driver is a row in an existing panel. That is the honest reading of invariant 12 rather than an exemption from it: the trap is about placed objects, and there were none.
  - Two things worth carrying into 38T:
    1. ⚠️ **An existing validator rule asked a harder question beats a new rule.** The one that mattered here was 38F's margin check with two more multipliers in it — a new rule would have duplicated the band and the two would have drifted. 38T moves demand for a bounded number of days: ask which existing rule now has to be evaluated *during a shock* before writing one about shocks.
    2. **A day-bounded multiplier is safe where a permanent one is not** — that is the whole reason a haggle may touch the sell side when standing may not. 38T's shortages and gluts are the same shape, and the bound is the same kind of thing: a saveable count of days, not a stronger clamp.
  - **Reachable when next played:** talk to Aldreth in the town square and take the trade line — under his standing there is a new line saying he might be talked down today, and a Haggle button beside it. Press it once: prices on both sides move, or he takes it badly and the villagers hear. Come back tomorrow. Gilda Ironmonger in the Embermarket and Marta Quill at the mine are the other two, and they are harder and dearer in that order.
- [ ] **38T — Caravan events + supply shocks** `[F/C]`
  - **Done when:** shortages, gluts and fairs move authored demand for a bounded number of days from a saveable service — never from `WorldEventDirector`, which is not `ISaveable`.
- [ ] **38U — Economy UI/UX pass** `[P]`
  - **Done when:** every price is explicable on hover, line by line, from the same functions the transaction charges.
- [ ] **38V — Validation, tests, docs, balance handoff** `[F/P]`
  - **Done when:** every new rule is negative-tested both ways, `ARCHITECTURE.md` gets its missing §2.x Economy section, `DESIGN.md` §6 is rewritten, and an `economy` dev command prints the realm's price landscape for Phase 56.

---
